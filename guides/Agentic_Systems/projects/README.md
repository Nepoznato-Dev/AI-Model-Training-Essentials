# Agentic Systems Projects

Runnable projects for Agentic AI Systems.

## Projects

### 1. Simple Agent with Memory

A basic agent implementation with short-term and long-term memory, tools (calculator, time lookup), and a perceive-think-act-observe loop.

**Files:**
- `simple_agent_memory/` - Agent with memory capabilities
  - `main.py` - Full agent with ShortTermMemory, LongTermMemory, tools, and interactive mode
  - `requirements.txt` - No external dependencies (stdlib only)

## Getting Started

```bash
# Install dependencies (each project has its own requirements.txt)
cd <project_name>
pip install -r requirements.txt

# Run a project
python main.py
```

## Requirements

See the `requirements.txt` file in each project folder.
