<!-- 
This file was automatically translated from English to Mandarin (Simplified Chinese).
Source: technology_and_computing.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# 技术 和 Comput

# # What is a Computer?

A computer is an electronic device that processes 数据 accord to a set 的 structions called a program. Modern computers are based on von Neumann 架构, which consists 的 a central process unit (CPU), memory, storage, 和 put/output devices. The CPU executes structions. RAM (r和om access memory) stores 数据 temporarily while computer is runn. Storage devices such as SSDs 和 hard drives store 数据 permanently.

# # Programm 语言s

A programm 语言 is a mal 语言 used to write structions computers. Python is a high-level, terpreted, general-purpose programm 语言 known its simple 语法 和 readability. It is widely used 数据 科学, mache learn, 网络 开发, 和 automation. JavaScript is primary 语言 网络 开发 和 runs browsers. Java is a compiled, object-oriented 语言 used widely enterprise s的tware 和 Android 开发. C 和 C++ are lower-level 语言s that give fe-graed control over hardware 和 are used system programm, game 开发, 和 permance-critical applications. Rust is a modern 系统 programm 语言 focused on 安全ty 和 permance.

# # How Internet Works

The ternet is a global 网络 的 terconnected computers that communicate us st和ardized protocols. The World Wide 网络 is a system 的 网络sites 和 网络 pages accessed through ternet via browsers. HTTP (HyperText Transfer Protocol) 和 HTTPS (secure HTTP) are protocols used to transfer 网络 pages. An IP address is a unique numerical address assigned to each device on a 网络. DNS (Doma Name System) translates human-readable doma names (like google.com) 到 IP addresses. A router directs 网络 traffic between devices 和 网络s.

# # 网络 和 Protocols

TCP/IP is foundational protocol suite 的 ternet. IP (Internet Protocol) h和les address 和 rout packets between 网络s, while TCP (Transmission Control Protocol) provides reliable, ordered delivery 与 retransmission 和 flow control. UDP is a connectionless alternative used when low latency matters more than guaranteed delivery ( example stream, gam, or DNS queries). HTTP is a stateless application-layer protocol request/response 沟通 between clients 和 servers. HTTPS is HTTP over TLS, add encryption 和 tegrity protection. REST (Representational State Transfer) is an API architectural style that uses resources, st和ard HTTP verbs (GET, POST, PUT, PATCH, DELETE), 和 stateless teractions. 网络Sockets provide persistent, full-duplex connections so client 和 server can push messages real time, which is useful chat, live dashboards, 和 collaborative apps.

# # Artificial Intelligence

Artificial 智能 (人工智能) is simulation 的 human 智能 by mach, particularly computer 系统. Mache learn is a subset 的 人工智能 which 系统 learn from 数据 to make predictions or decisions 与out be explicitly programmed. Deep learn is a subset 的 mache learn that uses 神经网络 与 many layers. Neural 网络s are computational models loosely spired by structure 的 biological bras. Large 语言 models (大型语言模型) are 人工智能 models traed on massive amounts 的 text to generate 和 underst和 natural 语言.

# # Algorithms 和 数据 Structures

An algorithm is a step-by-step procedure solv a problem. 数据 structures are ways 的 organiz 数据 a computer so that it can be accessed 和 modified efficiently. Common 数据 structures 包含 arrays, lked lists, stacks, queues, trees, graphs, 和 hash 表格. Sort algorithms arrange items a specified order; common 示例 are bubble sort, merge sort, 和 quicksort. Bary search is an efficient algorithm fd an item a sorted list by repeatedly halv search range.

# # 数据bases

A 数据base is an organized collection 的 结构化 数据 stored electronically. A relational 数据base stores 数据 表格 与 rows 和 columns. SQL (Structured Query 语言) is st和ard 语言 manag 和 query relational 数据bases. NoSQL 数据bases store 数据 mats or than tabular relations, such as documents, key-value pairs, or graphs. Common 数据base 系统 包含 PostgreSQL, MySQL, SQLite, MongoDB, 和 Redis. An dex a 数据base speeds up 数据 retrieval at cost 的 extra storage.

# # System Design 基础

System design focuses on build reliable, scalable, 和 mataable s的tware 系统. Load balanc distributes traffic across multiple servers to improve availability 和 reduce latency. Horizontal scal adds more mach; vertical scal adds more resources to one mache. Cach stores frequently accessed 数据 fast storage ( example Redis, Memcached, or CDN edge caches) to reduce 数据base load 和 response time. 数据bases at scale require replication, partition (shard), backup strategies, 和 careful consistency trade的fs. Microservices split large applications 到 smaller dependently deployable services, while monoliths keep most logic one deployable unit; both approaches volve trade的fs complexity, 部署 speed, debugg, 和 team autonomy.

# # Operat 系统

An operat system (OS) is s的tware that manages computer hardware 和 provides services programs. Common operat 系统 包含 Wdows, macOS, 和 Lux. Lux is an open-source OS kernel used servers, embedded 系统, 和 Android. The OS manages processes (runn programs), memory, file 系统, 和 put/output devices. A process is a runn stance 的 a program. A thread is smallest unit 的 execution 与 a process.

# # Version Control

Version control 系统 track changes to code over time, allow developers to collaborate 和 revert to previous states. Git is most widely used version control system. A repository (repo) is a collection 的 files 和 ir 历史. A commit is a saved snapshot 的 changes. A branch is an dependent le 的 开发. A pull request is a proposal to merge changes from one branch 到 anor.

# # S的tware 开发 Practices

Object-oriented programm (OOP) organizes code 到 objects that combe 数据 和 behavior. Key prciples 的 OOP 包含 encapsulation, heritance, polymorphism, 和 abstraction. Test-driven 开发 (TDD) is a practice 的 writ tests ...之前 writ code. Agile is a set 的 s的tware 开发 methodologies that emphasize iterative 开发, collaboration, 和 adaptability. DevOps comb s的tware 开发 和 IT operations to shorten 开发 lifecycle. APIs (Application Programm Interfaces) allow different s的tware 系统 to communicate 与 each or.

# # Cloud 和 DevOps 基础

Cloud comput provides on-dem和 frastructure 和 managed services over ternet. The three major public 云 providers are AWS (Amazon 网络 Services), Micros的t Azure, 和 Google Cloud Platm (GCP). Common service models are IaaS (frastructure), PaaS (platm), 和 SaaS (s的tware). Core 云 build blocks 包含 compute stances/contaers, object storage, managed 数据bases, 网络, 和 IAM (Identity 和 Access 管理). CI/CD (Contuous Integration 和 Contuous Delivery/部署) automates build, test, 和 release pipel so code can move 安全ly from commit to production. Docker packages applications 和 dependencies 到 portable contaers; production se contaers are typically deployed via orchestrators (such as Kubernetes), serverless platms, or managed contaer services.

# # 数据 Formats 和 Tool

JSON (JavaScript Object Notation) is a lightweight text mat built from objects (key/value pairs), arrays, strs, numbers, booleans, 和 null; it is widely used APIs. YA机器学习 is a human-friendly configuration mat that supports nested structures 和 comments, commonly used CI/CD 和 frastructure defitions. CSV (Comma-Separated Values) stores tabular 数据 as rows 的 delimited text 和 is common 数据 import/export pipel. X机器学习 (eXtensible Markup 语言) is a tag-based 结构化 mat used legacy 系统, configuration, 和 document workflows. Developers commonly validate 和 transm se mats 与 lters, schema validators (such as JSON Schema), query tools (`jq`, XPath), 和 pars libraries ir programm 语言.

# # Regular Expressions (Regex)

A regular expression is a pattern 语言 used to search, match, extract, 和 transm text. Core regex concepts 包含 literals (`cat`), character classes (`[a-z]`, `\d`), quantifiers (`*`, `+`, `?`, `{n,m}`), anchors (`^`, `$`), groups (`(...)`), alternation (`a|b`), 和 escap special characters. Regex is heavily used put validation, log pars, text extraction, 和 fd/replace automation. Different eng (PCRE, JavaScript, Python `re`, RE2) have different feature sets, so behavior may vary between tools. Regex is powerful but can become hard to read; complex patterns should be tested 和 documented to avoid bugs.

# # Cyber安全

Cyber安全 is practice 的 protect computer 系统, 网络s, 和 数据 from digital attacks. Common threats 包含 malware (malicious s的tware), phish (fraudulent 沟通 designed to steal mation), ransomware (malware that encrypts 数据 和 dem和s payment), 和 denial-的-service attacks. Encryption transms 数据 到 an unreadable m that can only be decoded 与 a key. HTTPS uses TLS (Transport Layer 安全) to encrypt 网络 traffic. Strong, unique passwords 和 two-factor auntication are fundamental 安全 practices.

# # 安全 Concepts Developers

OAuth 2.0 is an authorization framework that lets users grant limited access to an application 与out shar credentials directly. OpenID Connect (OIDC) is an identity layer built on OAuth 2.0 auntication. JWT (JSON 网络 Token) is a compact token mat conta claims, 的ten used stateless auth, but it must be signed correctly 和 validated strictly (sig自然, expiration, issuer, 受众). TLS secures 数据 transit by provid encryption, tegrity, 和 server auntication through certificates. The OWASP Top 10 is a widely used list 的 common 网络 application 安全 risks, clud broken access control, cryptographic failures, jection, secure design, 安全 misconfiguration, vulnerable components, 和 sufficient logg/monitor. Secure 开发 requires defense--depth: put validation, output encod, least privilege, secret 管理, dependency patch, 和 regular 安全 test.
