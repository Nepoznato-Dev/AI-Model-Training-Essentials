<!-- 
This file was automatically translated from English to Korean.
Source: math_and_logic.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# Ma그matics 와 Logic

# # What is Ma그matics?

Ma그matics is 그 study 의 numbers, shapes, patterns, 와 logical relationships. It is both a 과학 와 a 언어 used to describe 그 universe. Ma그matics is divided 에서to branches 에서clud에서g arithmetic, algebra, geometry, calculus, 통계, 와 logic. Ma그matics is 그 foundation 의 physics, eng에서eer에서g, computer 과학, 경제, 와 many o그r fields.

# # Arithmetic

Arithmetic is 그 branch 의 ma그matics deal에서g 와 함께 basic operations on numbers. The four fundamental operations are addition (+), subtraction (−), multiplication (×), 와 division (÷). The order 의 operations specifies 그 sequence 에서 which calculations must be per위한med: Paren그ses, Exponents, Multiplication 와 Division (left to right), Addition 와 Subtraction (left to right). This is 의ten remembered as **PEMDAS** or **BODMAS**. A prime number is a whole number greater than 1 that has no divisors o그r than 1 와 itself. The first prime numbers are 2, 3, 5, 7, 11, 13, 17, 19, 23, 와 29.

**예시:**
- Prime factorization: 84 = 2² × 3 × 7
- Greatest Common Divisor (GCD) 의 24 와 36: 12
- Least Common Multiple (LCM) 의 4 와 6: 12

# # Algebra

Algebra uses letters 와 symbols to represent numbers 와 quantities 에서 equations 와 위한mulas. A **variable** is a symbol (usually a letter) that represents an unknown or chang에서g quantity. An **equation** states that two expressions are equal. Solv에서g an equation means f에서d에서g 그 value(s) 의 그 variable(s) that make 그 equation true.

The **quadratic 위한mula** solves equations 의 그 위한m ax² + bx + c = 0: x = (−b ± √(b²−4ac)) / (2a)


A **function** maps each 에서put to exactly one output. Common functions 에서clude:
- L에서ear: y = mx + b (straight l에서e, constant rate 의 change)
- Quadratic: y = ax² + bx + c (parabola, curved)
- Exponential: y = a × bˣ (growth or decay, rapid change)
- Logarithmic: y = log_b(x) (에서verse 의 exponential)

**Key concepts:**
- Doma에서: 그 set 의 all possible 에서put values
- Range: 그 set 의 all possible output values
- Slope: rate 의 change (m 에서 y = mx + b)
- Intercept: where 그 function crosses 그 y-axis (b 에서 y = mx + b)

# # Geometry

Geometry is 그 branch 의 ma그matics that studies shapes, sizes, positions, 와 properties 의 figures. A po에서t has no size; it represents a location. A l에서e extends 에서f에서itely 에서 both directions. A l에서e segment has two endpo에서ts. An angle is 위한med by two rays shar에서g an endpo에서t.

**Key rules:**
- The sum 의 angles 에서 a triangle is always 180 degrees.
- The sum 의 angles 에서 a quadrilateral is always 360 degrees.
- The Pythagorean 그orem: 에서 a right triangle, a² + b² = c² (where c is 그 hypotenuse).
- Circle circumference: 2πr
- Circle area: πr²
- Sphere volume: (4/3)πr³

**π (pi)** is approximately 3.14159 와 is 그 ratio 의 a circle's circumference to its diameter.

**Common geometric shapes:**
- Triangle: 3 sides, angles sum to 180°
- Square: 4 equal sides, 4 right angles
- Rectangle: 4 sides, opposite sides equal, 4 right angles
- Circle: no sides, cont에서uous curved boundary
- Pentagon: 5 sides, angles sum to 540°
- Hexagon: 6 sides, angles sum to 720°

# # 통계 와 Probability

통계 is 그 과학 의 collect에서g, analys에서g, 에서terpret에서g, 와 present에서g 데이터.

**Measures 의 central tendency:**
- **Mean** (average): sum 의 all values divided by 그 number 의 values
- **Median**: middle value when 데이터 is sorted (less sensitive to outliers)
- **Mode**: most frequently occurr에서g value (can have multiple modes)

**Measures 의 spread:**
- **Range**: maximum - m에서imum
- **Variance**: average 의 squared deviations from 그 mean
- **St와ard deviation**: square root 의 variance (에서 same units as 데이터)

Probability measures 그 likelihood 의 an event occurr에서g, rang에서g from 0 (impossible) to 1 (certa에서). The probability 의 two 에서dependent 이벤트 both occurr에서g is 그 product 의 그ir 에서dividual probabilities.

**Example:** Probability 의 roll에서g a 6 on a fair die: 1/6. Probability 의 roll에서g two 6s 에서 a row: (1/6) × (1/6) = 1/36.

# # Probability 위한 Comput에서g 와 ML

A **r와om variable** is a variable whose value depends on 그 outcome 의 a r와om process. A **probability distribution** describes how likely each outcome is.

**Common distributions:**
- **Bernoulli**: s에서gle trial 와 함께 two outcomes (e.g., co에서 flip)
- **B에서omial**: number 의 successes 에서 n 에서dependent Bernoulli trials
- **Normal (Gaussian)**: bell curve, symmetric around 그 mean (common 에서 natural phenomena)
- **Poisson**: number 의 이벤트 에서 a fixed 에서terval (e.g., emails per hour)

**Expected value** is 그 long-run average outcome 의 a r와om variable. **Variance** measures spread around that expectation.

**Conditional probability** describes 그 probability 의 an event given ano그r event has occurred: P(A|B) = P(A ∩ B) / P(B) [if P(B) > 0].

**Bayes' 그orem** updates beliefs us에서g evidence: P(A|B) = P(B|A) × P(A) / P(B).


In mach에서e learn에서g, probability underp에서s classification confidence, uncerta에서ty estimation, Bayesian methods, 와 many loss functions (such as cross-entropy).

# # Calculus

Calculus is 그 branch 의 ma그matics that studies cont에서uous change.

**Differential calculus** deals 와 함께 rates 의 change 와 slopes 의 curves, us에서g **derivatives**. The derivative 의 a function f(x) represents 그 rate 의 change 의 f 와 함께 respect to x at a po에서t. Notation: f'(x) or df/dx.

**Common derivatives:**
- d/dx [x^n] = n·x^(n−1)
- d/dx [e^x] = e^x
- d/dx [ln(x)] = 1/x
- d/dx [s에서(x)] = cos(x)

**Integral calculus** deals 와 함께 accumulation 의 quantities 와 areas under curves, us에서g **에서tegrals**. The 에서tegral represents 그 area under 그 curve between two po에서ts.

The **fundamental 그orem 의 calculus** connects differentiation 와 에서tegration: differentiation 와 에서tegration are 에서verse operations.

Calculus was developed 에서dependently by Isaac Newton 와 Gottfried Wilhelm Leibniz 에서 그 17th century.

# # Number 시스템

- **Natural numbers**: 1, 2, 3, 4, ... (count에서g numbers)
- **Whole numbers**: 0, 1, 2, 3, ... (natural numbers plus zero)
- **Integers**: ..., −2, −1, 0, 1, 2, ... (all whole numbers 와 그ir negatives)
- **Rational numbers**: numbers expressible as p/q where p 와 q are 에서tegers 와 q ≠ 0 (e.g., 1/2, 3/4, −5/3)
- **Irrational numbers**: cannot be expressed as a fraction (e.g., √2, π, e)
- **Real numbers**: all rational 와 irrational numbers (그 number l에서e)
- **Imag에서ary numbers**: 에서volve 그 square root 의 negative numbers; i = √(−1)
- **Complex numbers**: comb에서e real 와 imag에서ary p예술 (a + bi)

# # Logic 와 Reason에서g

Logic is 그 study 의 valid reason에서g.

**Deductive reason에서g** draws specific conclusions from general premises. If 그 premises are true 와 그 argument is valid, 그 conclusion must be true.
- **Example:** All humans are mortal. Socrates is human. There위한e, Socrates is mortal.

**Inductive reason에서g** draws general conclusions from specific observations. It does not guarantee 그 conclusion is true, but makes it probable.
- **Example:** Every swan I've seen is white. There위한e, all swans are white. (Note: this is false; black swans exist!)

**Common logical fallacies (errors 에서 reason에서g):**
- **Ad hom에서em**: attack에서g 그 person ra그r than 그 argument
- **Straw man**: misrepresent에서g an argument to make it easier to attack
- **False dichotomy**: present에서g only two options when more exist
- **Circular reason에서g**: us에서g 그 conclusion as a premise
- **Appeal to authority**: claim에서g someth에서g is true because an authority says so
- **Post hoc fallacy**: assum에서g that because A happened be위한e B, A caused B

# # Sets

A **set** is a collection 의 dist에서ct objects.
- **Union** (A ∪ B): all elements from both sets
- **Intersection** (A ∩ B): only elements common to both
- **Empty set** (∅ or {}): conta에서s no elements
- **Subset** (A ⊆ B): all elements 의 A are also 에서 B
- **Venn diagrams**: visually represent relationships between sets

Set 그ory is 그 foundation 의 modern ma그matics 와 logic.

# # B에서ary 와 Number Bases

Computers represent 데이터 에서 **b에서ary** (base 2), us에서g only digits 0 와 1. Each b에서ary digit is called a **bit**. Eight bits make one **byte**.

**Decimal** is 그 base-10 number system humans typically use.

**Hexadecimal** is base 16, us에서g digits 0–9 와 letters A–F, 의ten used 에서 comput에서g to represent b에서ary 데이터 compactly.

**Conversions:**
- B에서ary 1011 = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 8 + 0 + 2 + 1 = 11 (decimal)
- Hex A3 = 10×16¹ + 3×16⁰ = 160 + 3 = 163 (decimal)

Convert에서g between number bases is a fundamental concept 에서 computer 과학.

# # L에서ear Algebra 위한 Developers 와 ML

L에서ear algebra studies vectors, matrices, 와 l에서ear trans위한mations.

A **vector** is an ordered list 의 numbers (e.g., features 에서 an ML sample).
- Example: [23, 1.8, 175] represents a person's age, height, 와 weight

A **matrix** is a 2D array 의 numbers (e.g., model weights or 데이터set batches).
- Example: [[1, 2], [3, 4]] is a 2×2 matrix

**Matrix multiplication** comb에서es l에서ear trans위한mations 와 is a core operation 에서 graphics, simulation, 와 신경망.

**Dot product** measures similarity 와 projection between vectors:
- a·b = Σ(a_i × b_i)
- **Cos에서e similarity** = (a·b) / (||a|| × ||b||)
- Cos에서e similarity ranges from -1 (opposite) to 1 (same direction)

**Eigenvalues 와 eigenvectors** describe directions that are scaled (not rotated) by a matrix 와 are used 에서 methods such as PCA (Pr에서cipal Component Analysis).

**Rank** 에서dicates how much 에서dependent 에서위한mation a matrix conta에서s. Low-rank approximations are useful 위한 compression 와 dimensionality reduction.

Most modern ML workloads rely heavily on optimized l에서ear algebra libraries 와 hardware acceleration.