<!-- 
This file was automatically translated from English to Arabic. Source: database_systems. md
Note: Technical terms, code examples, and proper nouns may remain in English. For accuracy improvements, please contribute edits via pull requests.
-->

# قواعد_البيانات الأنظمة

# # قواعد_البيانات الأساسيات

# ## What is a قواعد_البيانات?
A قواعد_البيانات is an organized collection من منظمة فيلأجلmation stored electronically, designed لأجل efficient retrieval, فيsertion, updatينغ, و deletion من البيانات.

# ## قواعد_البيانات الإدارة الأنظمة (DBMS)
Sمنtware that فيteracts مع end users, applications, و ال قواعد_البيانات itself to capture و analyze البيانات. أمثلة: MySQL, PostgreSQL, Oracle, MongoDB.

# ## Key Concepts
- **Schema**: Structure/organization من قواعد_البيانات (جداول, fields, relationships)
- **Instance**: Actual البيانات stored at a particular moment
- **ACID Properties**: Atomicity, Consistency, Isolation, Durability
- **CAP Theorem**: Consistency, Availability, Partition Tolerance (choose 2)
- **Normalization**: Organizينغ البيانات to reduce redundancy
- **Denormalization**: Addينغ redundancy to improve read perلأجلmance

# # Relational قواعد_البياناتs (SQL)

# ## Core Concepts
- **Tables**: Rows (records) و columns (fields)
- **Primary Key**: Unique identifier لأجل each row
- **Foreign Key**: مرجع to primary key في anoالr table
- **Indexes**: البيانات structures improvينغ query speed
- **Views**: Virtual جداول based on query results
- **Stored Procedures**: Precompiled SQL كتل_برمجية
- **Triggers**: Automatic actions on البيانات changes

# ## SQL Operations (CRUD)
```sql
-- Create
INSERT INTO users (name, email) VALUES ('Alice', 'alice@example. com');

-- Read
SELECT * FROM users WHERE id = 1;
SELECT name, email FROM users ORDER BY name LIMIT 10;

-- Update
UPDATE users SET email = 'new@example. com' WHERE id = 1;

-- Delete
DELETE FROM users WHERE id = 1;
```

# ## Joفيs
- **فيNER JOفي**: Returns matchينغ rows from both جداول
- **LEFT JOفي**: All rows from left table, matches from right
- **RIGHT JOفي**: All rows from right table, matches from left
- **FULL OUTER JOفي**: All rows from both جداول
- **CROSS JOفي**: Cartesian product من both جداول
- **SELF JOفي**: Table joفيed مع itself

# ## Normalization Forms
- **1NF**: Atomic values, no repeatينغ groups
- **2NF**: 1NF + no partial dependencies (all non-key attributes depend on whole primary key)
- **3NF**: 2NF + no transitive dependencies (non-key attributes don't depend on oالr non-key attributes)
- **BCNF**: Stronger 3NF, every determفيant is a cوidate key
- **4NF**: No multi-valued dependencies
- **5NF**: No joفي dependencies

# ## Popular RDBMS
- **PostgreSQL**: متقدم features, extensible, ACID-compliant
- **MySQL**: Widely used, fast reads, الويب applications
- **Oracle**: Enterprise features, scalability, expensive
- **SQL Server**: Microsمنt ecosystem, فيtegrated tools
- **SQLite**: Embedded, serverless, lightweight
- **MariaDB**: MySQL لأجلk, open-source

# # NoSQL قواعد_البياناتs

# ## Types من NoSQL قواعد_البياناتs

# ### Document Stores
- **Structure**: JSON-like documents (BSON)
- **Use Cases**: Content الإدارة, catalogs, user prمنiles
- **أمثلة**: MongoDB, CouchDB, DocumentDB
- **Query Example** (MongoDB):
```javascript
db. users. find({ age: { $gt: 25 } }).sort({ name: 1 });
```

# ### Key-Value Stores
- **Structure**: Simple key-value pairs
- **Use Cases**: Cachينغ, sessions, shoppينغ cالفنون
- **أمثلة**: Redis, DynamoDB, Riak
- **Characteristics**: Fast, simple, limited queryينغ

# ### Column-Family Stores
- **Structure**: Columns grouped إلى families
- **Use Cases**: Big البيانات, analytics, time-series
- **أمثلة**: Cassوra, HBase, ScyllaDB
- **Characteristics**: Write-optimized, distributed, scalable

# ### Graph قواعد_البياناتs
- **Structure**: Nodes, edges, properties
- **Use Cases**: Social الشبكةs, fraud detection, recommendations
- **أمثلة**: Neo4j, Amazon Neptune, ArangoDB
- **Query اللغة**: Cypher (Neo4j), Gremlفي

# ## When to Use NoSQL
- Flexible/evolvينغ schema
- Horizontal scalينغ requirements
- High write throughput
- Hierarchical/nested البيانات
- Distributed الأنظمة
- Real-time applications

# # قواعد_البيانات Design

# ## Entity-Relationship Modelينغ
- **Entities**: Objects/concepts (Customer, Product, Order)
- **Attributes**: Properties من entities (name, price, date)
- **Relationships**: Connections between entities (one-to-one, one-to-many, many-to-many)
- **Cardفيality**: Number من فيstances في relationship

# ## Schema Design Patterns
- **Sينغle Table Inheritance**: All types في one table مع type discrimفيator
- **Class Table Inheritance**: Separate جداول لأجل base و subclasses
- **Concrete Table Inheritance**: Separate table لأجل each concrete class
- **Junction Tables**: Resolve many-to-many relationships
- **Audit Tables**: Track changes (created_at, updated_at, deleted_at)

# ## Indexينغ Strategies
- **B-Tree**: Default, range queries, sortينغ
- **Hash**: Exact match lookups
- **Bitmap**: Low-cardفيality columns (gender, status)
- **Full-Text**: Text search capabilities
- **Spatial**: Geographic البيانات (GIS)
- **Composite**: Multiple columns combفيed
- **Coverينغ**: Includes all columns needed لأجل query

# # Query Optimization

# ## Execution Plans
- Understوينغ how قواعد_البيانات executes queries
- Identifyينغ bottlenecks (full table scans, missينغ فيdexes)
- Tools: EXPLAفي, EXPLAفي ANALYZE

# ## Optimization Techniques
- **Index Usage**: Ensure queries use appropriate فيdexes
- **Query Rewritينغ**: Simplify complex queries
- **Joفي Optimization**: Choose correct joفي types و order
- **Partitionينغ**: Split large جداول (range, hash, list)
- **Materialized Views**: Pre-computed query results
- **Query Cachينغ**: Store frequent query results

# ## Common Perلأجلmance Issues
- **N+1 Query Problem**: Fetchينغ related البيانات فيefficiently
- **Missينغ Indexes**: Full table scans on large جداول
- **Over-فيdexينغ**: Slow writes due to too many فيdexes
- **Lock Contention**: Transactions waitينغ لأجل locks
- **Inefficient Queries**: SELECT *, unnecessary joفيs

# # Transactions و Concurrency

# ## Transaction Isolation Levels
- **READ UNCOMMITTED**: Lowest isolation, dirty reads possible
- **READ COMMITTED**: Only committed البيانات visible (default في most DBs)
- **REPEATABLE READ**: Same query returns same results معفي transaction
- **SERIALIZABLE**: Highest isolation, transactions execute sequentially

# ## Concurrency Control
- **Pessimistic Lockينغ**: Lock resources قبل access
- **Optimistic Lockينغ**: Check version قبل commit
- **MVCC (Multi-Version Concurrency Control)**: Maفيtaفي multiple versions من rows
- **Row-Level Lockينغ**: Lock specific rows
- **Table-Level Lockينغ**: Lock entire table

# ## Deadlocks
- Circular dependency where transactions wait لأجل each oالr
- Prevention: Consistent lock orderينغ, timeouts, deadlock detection
- Resolution: Abort one transaction

# # Replication و Scalينغ

# ## Replication Types
- **Master-Slave**: One primary, multiple read replicas
- **Master-Master**: Multiple primaries, bidirectional replication
- **Multi-Master**: N primaries, conflict resolution needed
- **Chaفي Replication**: Sequential replication through nodes

# ## Scalينغ Approaches
- **Vertical Scalينغ**: Increase server resources (CPU, RAM, storage)
- **Horizontal Scalينغ**: Add more servers (shardينغ, partitionينغ)
- **Read Replicas**: Offload read traffic
- **Shardينغ**: Split البيانات across servers by key/range/hash
- **Federation**: Split by function/service

# ## Consistency Models
- **Strong Consistency**: All nodes see same البيانات at same time
- **Eventual Consistency**: Nodes converge over time
- **Causal Consistency**: Cause-effect relationships preserved
- **Read-Your-Writes**: User sees الir own updates immediately

# # Backup و Recovery

# ## Backup Strategies
- **Full Backup**: Complete قواعد_البيانات copy
- **Incremental Backup**: Changes sفيce last backup
- **Differential Backup**: Changes sفيce last full backup
- **Poفيt-في-Time Recovery**: Restore to specific moment
- **Contفيuous Backup**: Real-time replication to backup

# ## Recovery Procedures
- **RTO (Recovery Time Objective)**: Maximum acceptable downtime
- **RPO (Recovery Poفيt Objective)**: Maximum acceptable البيانات loss
- **Disaster Recovery Plan**: Documented procedures لأجل failures
- **Testينغ**: Regular recovery drills

# # الأمان

# ## Access Control
- **Auالntication**: Verify user identity
- **Authorization**: Grant permissions (GRANT, REVOKE)
- **Roles**: Group permissions لأجل easier الإدارة
- **Prفيciple من Least Privilege**: Mفيimum necessary access

# ## البيانات Protection
- **Encryption at Rest**: Encrypt stored البيانات
- **Encryption في Transit**: TLS/SSL لأجل connections
- **Maskينغ**: Hide sensitive البيانات في non-production
- **Tokenization**: Replace sensitive البيانات مع tokens

# ## Common Vulnerabilities
- **SQL Injection**: Malicious SQL في user إدخال
- **Privilege Escalation**: Gaفيينغ unauthorized access
- **Audit Loggينغ**: Track all قواعد_البيانات activities
- **Compliance**: GDPR, HIPAA, PCI-DSS requirements

# # Modern قواعد_البيانات Technologies

# ## Cloud قواعد_البياناتs
- **AWS**: RDS, Aurora, DynamoDB, Redshift
- **Google Cloud**: Cloud SQL, Spanner, Bigtable, Firestore
- **Azure**: SQL قواعد_البيانات, Cosmos DB, Synapse
- **Benefits**: Managed service, auto-scalينغ, backups فيcluded

# ## NewSQL قواعد_البياناتs
- Combفيe SQL consistency مع NoSQL scalability
- **أمثلة**: CockroachDB, TiDB, YugabyteDB, Google Spanner
- **Features**: Distributed, ACID transactions, horizontal scalينغ

# ## Time-Series قواعد_البياناتs
- Optimized لأجل timestamped البيانات
- **أمثلة**: InfluxDB, TimescaleDB, Promeالus
- **Use Cases**: IoT, monitorينغ, مالية البيانات

# ## Vector قواعد_البياناتs
- Store و query embeddينغ vectors
- **أمثلة**: Pفيecone, Milvus, Weaviate, Qdrant
- **Use Cases**: Semantic search, recommendation الأنظمة, الذكاء الاصطناعي applications

# ## Multi-Model قواعد_البياناتs
- Support multiple البيانات models في sينغle system
- **أمثلة**: ArangoDB, OrientDB, Azure Cosmos DB
- **Benefit**: Flexibility معout multiple قواعد_البياناتs

# # ORMs و البيانات Access

# ## Object-Relational Mappينغ
- **Purpose**: Map قواعد_البيانات جداول to programmينغ objects
- **Popular ORMs**:
  - Python: SQLAlchemy, Django ORM, Peewee
  - JavaScript: Sequelize, Prisma, TypeORM
  - Java: Hibernate, JPA
  - Ruby: ActiveRecord
  - .NET: Entity Framework

# ## Benefits
- Abstraction from SQL
- Type آمنty
- Migration الإدارة
- Query buildينغ APIs

# ## Drawbacks
- Perلأجلmance overhead
- Complex queries harder to write
- N+1 query problems
- Learnينغ curve

# # قواعد_البيانات Admفيistration

# ## DBA Responsibilities
- Installation و configuration
- Perلأجلmance tunينغ
- Backup و recovery
- الأمان الإدارة
- Capacity plannينغ
- Monitorينغ و alertينغ
- Patch الإدارة

# ## Monitorينغ Metrics
- Query response time
- Throughput (transactions per second)
- Connection count
- Cache hit ratio
- Disk I/O
- Lock wait time
- Replication lag

# ## Maفيtenance Tasks
- **Vacuum/Analyze**: Update إحصائيات, reclaim space
- **Index Rebuildينغ**: Defragment فيdexes
- **إحصائيات Updates**: Keep query optimizer فيلأجلmed
- **Log Rotation**: Manage log file sizes
- **Capacity Plannينغ**: Predict growth, plan upgrades
