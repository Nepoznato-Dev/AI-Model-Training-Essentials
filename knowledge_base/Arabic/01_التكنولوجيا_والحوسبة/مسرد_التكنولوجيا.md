<!-- 
This file was automatically translated from English to Arabic. Source: technology_glossary. md
Note: Technical terms, code examples, and proper nouns may remain in English. For accuracy improvements, please contribute edits via pull requests.
-->

# التكنولوجيا Glossary

A مرجع glossary تغطي الذكاء الاصطناعي models, hardware, benchmarks, و core concepts
في ال modern الذكاء الاصطناعي و computينغ lوscape.

---

# # الذكاء الاصطناعي اللغة Models و Assistants

# ## ChatGPT
ChatGPT is an الذكاء الاصطناعي chatbot developed by Openالذكاء الاصطناعي, first released في November 2022. It is powered by ال GPT series من large اللغة models (نماذج لغوية كبيرة). ChatGPT is one
من ال fastest-growينغ consumer الذكاء الاصطناعي products في التاريخ, reachينغ 100 million
users معفي two months من launch. It supports text-based conversation, code
generation, summarisation, و creative writينغ. Paid tiers provide access to
more powerful models such as GPT-4 و GPT-4o.

# ## GPT (Generative Pre-traفيed Transلأجلmer)
GPT is a family من large اللغة models created by Openالذكاء الاصطناعي. The العمارة
uses a decoder-only Transلأجلmer traفيed مع a next-token prediction objective on
massive text corpora. Key versions فيclude GPT-2 (2019, 1. 5B parameters, notable
لأجل "too dangerous to release" publicity), GPT-3 (2020, 175B parameters, widely
used via ال API), GPT-3. 5 (ال backbone من ال origفيal ChatGPT), و GPT-4
(2023, multimodal, perلأجلmance close to human expert level on many benchmarks).

# ## Claude
Claude is an الذكاء الاصطناعي assistant developed by Anthropic. It is named after Claude
Shannon, ال founder من فيلأجلmation الory. Anthropic was founded by لأجلmer
Openالذكاء الاصطناعي researchers و focuses on "constitutional الذكاء الاصطناعي" — a technique to make
models آمنr by traفيينغ الm to follow a set من prفيciples. Claude models
(Claude 1, 2, 3 Haiku / Sonnet / Opus) are known لأجل long context wفيdows (up
to 200,000 tokens), nuanced reasonينغ, و reduced harmful output compared to
baselفيe نماذج لغوية كبيرة.

# ## Gemفيi
Gemفيi is Google DeepMفيd's family من multimodal الذكاء الاصطناعي models, announced في
December 2023. Gemفيi is natively multimodal — traفيed from ال ground up on
text, images, audio, و video simultaneously, unlike earlier models that had
modalities added via fفيe-tunينغ. Versions فيclude Gemفيi Nano (on-device),
Gemفيi Flash (fast, cost-efficient), و Gemفيi Ultra (highest-capability).
Gemفيi powers Google's الذكاء الاصطناعي chatbot Bard (renamed Gemفيi) و Google Search الذكاء الاصطناعي
نظرة عامةs.

# ## Phi-3-mفيi
Phi-3-mفيi is a small اللغة model (SLM) developed by Microsمنt مع 3. 8B
parameters. It was released في April 2024. Unlike most large models, Phi-3-mفيi
was traفيed on a carefully curated "textbook-quality" البياناتset — a technique
pioneered by Microsمنt Research — that prioritises البيانات quality over raw volume. Despite beينغ far smaller than GPT-4 or Claude 3 Opus, Phi-3-mفيi matches or
outperلأجلms models several times larger on reasonينغ benchmarks such as Mتعلم آليU و
HumanEval. It supports a 4k token context wفيdow في its base variant و a 128k
wفيdow في ال long-context variant. Phi-3-mفيi can run on a sينغle consumer GPU
or even on-device on a modern smartphone مع sufficient RAM.

# ## Llama (Meta الذكاء الاصطناعي)
Llama (Large اللغة Model Meta الذكاء الاصطناعي) is an open-weights family من models
released by Meta. Llama 2 (2023) was released لأجل research و commercial use
مع sizes rangينغ from 7B to 70B parameters. Llama 3 (2024) improved
perلأجلmance significantly, مع models rangينغ from 8B to 70B (و later 400B+).
Because ال weights are publicly downloadable, Llama models are ال foundation
لأجل a large ecosystem من fفيe-tuned variants (Mistral, Alpaca, Vicuna, etc.)
و are widely used لأجل local/private الذكاء الاصطناعي النشرs.

# ## Mistral
Mistral الذكاء الاصطناعي is a French الذكاء الاصطناعي company that develops open و proprietary نماذج لغوية كبيرة. Mistral 7B (2023) demonstrated that a 7B-parameter model can match ال
perلأجلmance من much larger models usينغ efficient techniques such as slidينغ
wفيdow attention و grouped-query attention. Mixtral 8x7B (2024) is a mixture-
من-experts model — it routes each token to a subset من 8 expert الشبكةs,
achievينغ GPT-3. 5-level perلأجلmance while beينغ computationally cheaper. Mistral's models are fully open-weight و can be run locally.

---

# # GPU Hardware و Graphics Cards

# ## GPU (Graphics Processينغ Unit)
A GPU is a processor designed لأجل massively parallel computation. Origفيally
built لأجل renderينغ 3D graphics, GPUs have become essential لأجل الذكاء الاصطناعي/تعلم آلي traفيينغ
و فيference because الy can perلأجلm thousوs من floatينغ-poفيt operations
simultaneously usينغ thousوs من small cores. The two maفي GPU manufacturers
لأجل الذكاء الاصطناعي are NVIDIA و AMD.

# ## NVIDIA GeForce RTX Series
The RTX (Ray Tracينغ Texel eXtreme) series is NVIDIA's consumer GPU lفيe. RTX
30xx (Ampere, 2020) و RTX 40xx (Ada Lovelace, 2022) generations فيclude
dedicated Tensor Cores لأجل acceleratينغ الذكاء الاصطناعي operations. VRAM (video RAM) is
critical لأجل runnينغ الذكاء الاصطناعي models locally — an 8GB GPU can hوle 7B parameter
models في 4-bit quantisation; a 24GB GPU can hوle 70B models في 4-bit.

# ## NVIDIA A-Series و H-Series (البيانات Centre)
The A100 (Ampere, 2020) و H100 (Hopper, 2022) are NVIDIA's مهنية الذكاء الاصطناعي
accelerators. An H100 has up to 80GB من HBM3 memory و is ال stوard
hardware behفيd most large-scale LLM traفيينغ today. These GPUs cost $25,000–
$40,000 each but منfer 10–30× ال الذكاء الاصطناعي throughput من consumer RTX cards.

# ## AMD Radeon RX Series
AMD's consumer GPU lفيe. The RX 7900 XTX (2022) has 24GB VRAM و can run
local نماذج لغوية كبيرة via ROCm (AMD's GPU compute stack). AMD GPUs are generally less
well-supported than NVIDIA لأجل الذكاء الاصطناعي frameworks, though support is improvينغ.

# ## Intel Arc
Intel Arc is Intel's discrete GPU product lفيe, released startينغ في 2022. Arc
GPUs support XeSS (Intel's super-samplينغ) و have limited but growينغ support
لأجل الذكاء الاصطناعي فيference tasks via OpenVفيO و IPEX-LLM frameworks.

# ## ARK Intel (ark. فيtel. com)
ARK is Intel's منficial product specifications قواعد_البيانات at ark. فيtel. com. It
provides detailed technical specifications لأجل every Intel CPU, GPU, FPGA, و
NUC product, بما في ذلك core counts, clock speeds, TDP, supported memory types,
و فيstruction-set features. When you hear "check ARK لأجل specs," it means
visitينغ that قواعد_البيانات لأجل authoritative hardware فيلأجلmation.

---

# # الذكاء الاصطناعي Perلأجلmance Benchmarks

# ## Mتعلم آليU (Massive Multitask اللغة Understوينغ)
Mتعلم آليU is a benchmark testينغ LLM knowledge across 57 academic subjects بما في ذلك
maالmatics, التاريخ, القانون, medicفيe, و computer العلوم. It consists من
multiple-choice questions drawn from real university-level exams. A score من
70% is roughly human undergraduate level; GPT-4 و Claude 3 score above 86%.
Phi-3-mفيi scores around 70% despite its small size.

# ## HumanEval
HumanEval is Openالذكاء الاصطناعي's benchmark لأجل code generation. It consists من 164 Python
programmينغ problems مع automated test cases. Models are measured on
pass@k — ال probability that at least one من k generated solutions passes all
tests. GPT-4 scores ~87% (pass@1); a well-tuned 7B model can reach ~50–60%.

# ## HellaSwag
HellaSwag is a commonsense reasonينغ benchmark. Models are given a sentence
describينغ a mundane activity و must choose ال most likely contفيuation from
four options. The فيcorrect options are specially designed to be plausible but
subtly wrong. It tests wheالr a model has a grounded understوينغ من physical
و social situations.

# ## ARC (الذكاء الاصطناعي2 Reasonينغ Challenge)
ARC is a benchmark from ال Allen Institute لأجل الذكاء الاصطناعي. It consists من grade-school
العلوم questions, split إلى "Easy" و "Challenge" sets. The Challenge set
contaفيs questions that retrieval-based methods و simple statistical models
struggle مع, requirينغ multi-step reasonينغ.

---

# # Core الذكاء الاصطناعي/تعلم آلي Concepts

# ## RAG (Retrieval-Augmented Generation)
RAG is a technique that combفيes a retrieval system (typically a vector
قواعد_البيانات) مع a اللغة model. Instead من relyينغ solely on ال model's
parametric knowledge, RAG first retrieves relevant documents from an external
قاعدة المعرفة و الn فيcludes الm في ال model's context. This allows ال
model to answer questions about up-to-date or domaفي-specific فيلأجلmation
معout retraفيينغ. Potato. ai uses a لأجلm من RAG — it retrieves from its KB
و فيcludes ال results في ال context قبل generatينغ a response.

# ## Fفيe-tunينغ
Fفيe-tunينغ is ال process من contفيuينغ to traفي a pre-traفيed model on a
smaller, domaفي-specific البياناتset. This adapts ال model's weights لأجل a
particular task or domaفي. For example, a base LLM might be fفيe-tuned on
medical records to create a medical Q&A assistant. Fفيe-tunينغ is
computationally expensive but much cheaper than traفيينغ from scratch.

# ## Quantisation
Quantisation reduces ال numerical precision من model weights (e. g. from 32-bit
float to 4-bit فيteger). This dramatically reduces memory footprفيt — a 7B model
في 16-bit precision requires ~14GB VRAM; ال same model في 4-bit (GGUF لأجلmat)
requires ~4GB. Quantisation typically causes a small but acceptable accuracy
degradation و is ال maفي technique enablينغ large models to run on consumer
hardware or even mobile devices.

# ## Context Wفيdow
The context wفيdow is ال maximum number من tokens a model can process at once,
بما في ذلك both ال prompt و ال generated response. GPT-3. 5 had a 4,096-token
wفيdow; GPT-4 Turbo و Claude 3 support 128,000 tokens; Gemفيi 1. 5 Pro
supports 1,000,000 tokens. A larger context wفيdow allows ال model to "see"
more من a conversation or document at once, improvينغ coherence over long
exchanges.

# ## RLHF (Reفيلأجلcement Learnينغ from Human م_ملاحظات)
RLHF is ال traفيينغ technique that transلأجلms a base اللغة model (which
simply predicts ال next token) إلى an assistant that follows فيstructions و
behaves helpfully. Human raters score model outputs, a reward model is traفيed
on الir pمرجعs, و ال اللغة model is الn optimised ضد this
reward model usينغ reفيلأجلcement learnينغ. ChatGPT, Claude, و Gemفيi all use
variants من RLHF or similar alignment techniques (e. g. Constitutional الذكاء الاصطناعي,
Direct Pمرجع Optimisation).

# ## Transلأجلmer العمارة
The Transلأجلmer is ال neural الشبكة العمارة underlyينغ all modern نماذج لغوية كبيرة. Introduced في ال 2017 paper "Attention Is All You Need" by Vaswani et al., it
uses self-attention mechanisms to process all tokens في parallel raالr than
sequentially. Encoder-only Transلأجلmers (BERT) are used لأجل understوينغ tasks;
decoder-only Transلأجلmers (GPT, Llama, Mistral) are used لأجل generation tasks;
encoder-decoder Transلأجلmers (T5, BART) are used لأجل translation و summarisation.

# ## Embeddينغs و Vector قواعد_البياناتs
Embeddينغs are dense numerical representations من text (or images) produced by
a neural الشبكة. Semantically similar texts have embeddينغs that are close في
vector space. Vector قواعد_البياناتs (ChromaDB, Pفيecone, Weaviate, Qdrant) store
الse embeddينغs و support fast approximate nearest-neighbour search. They are
ال storage backbone من RAG الأنظمة, بما في ذلك Potato. ai's cold-memory layer.
