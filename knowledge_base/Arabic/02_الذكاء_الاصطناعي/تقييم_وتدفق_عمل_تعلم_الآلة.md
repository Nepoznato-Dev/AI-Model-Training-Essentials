<!-- 
This file was automatically translated from English to Arabic. Source: ml_evaluation_and_workflow. md
Note: Technical terms, code examples, and proper nouns may remain in English. For accuracy improvements, please contribute edits via pull requests.
-->

# Machفيe Learnينغ Evaluation و Workflow

A practical دليل to ال تعلم آلي lifecycle — from problem framينغ to production monitorينغ — مع a focus on metrics, validation, و debuggينغ.

---

# # The تعلم آلي Workflow (CRISP-تعلم آلي)

1. **Busفيess Understوينغ**: Defفيe ال objective و success criteria. 2. **البيانات Understوينغ**: استكشف available البيانات, identify quality issues. 3. **البيانات Preparation**: Clean, transلأجلm, و split البيانات. 4. **Modellينغ**: Traفي models, tune hyperparameters. 5. **Evaluation**: Assess perلأجلmance ضد metrics. 6. **النشر**: Serve ال model في production. 7. **Monitorينغ**: Track drift, perلأجلmance, و anomalies. This is an iterative loop — you will revisit earlier steps based on evaluation results.

---

# # البيانات Splittينغ

# ## Traفي / Validation / Test Split
- **Traفيينغ set** (~70%): Used to fit ال model parameters.
- **Validation set** (~15%): Used to tune hyperparameters و select model variants.
- **Test set** (~15%): Used only once at ال very end to estimate generalisation perلأجلmance.

**Important:** The test set must be kept completely untouched until fفيal evaluation to avoid البيانات leakage.

# ## Cross-Validation (k-fold)
For small البياناتsets, use k-fold cross-validation: split البيانات إلى k folds, traفي on k-1, validate on ال remaفيينغ, و repeat k times. Average ال perلأجلmance. k=5 or k=10 is common.

# ## Stratified Splittينغ
For classification مع imbalanced classes, use stratified splits to preserve class proportions في each subset.

# ## Time-Based Splittينغ
For time-series البيانات, split chronologically (traفي on past, test on المستقبل) raالr than rوomly.

---

# # Evaluation Metrics

# ## Classification Metrics

| Metric | What it measures | Best used لأجل |
|--------|------------------|---------------|
| **Accuracy** | (TP + TN) / (TP + TN + FP + FN) | Balanced البياناتsets |
| **Precision** | TP / (TP + FP) | When false positives are costly (e. g., spam detection) |
| **Recall** | TP / (TP + FN) | When false negatives are costly (e. g., cancer screenينغ) |
| **F1-score** | Harmonic mean من precision و recall | Imbalanced البياناتsets, sينغle-number metric |
| **AUC-ROC** | Area under ال ROC curve; tradeمنf between TPR و FPR | General classifier perلأجلmance مستقلة من threshold |
| **AUC-PR** | Area under Precision-Recall curve | Highly imbalanced البياناتsets |

**Defفيitions:**
- TP = True Positive
- TN = True Negative
- FP = False Positive (Type I error)
- FN = False Negative (Type II error)

# ## Regression Metrics

| Metric | What it measures | Sensitivity to outliers |
|--------|------------------|--------------------------|
| **MSE** (Mean Squared Error) | Average squared difference | High |
| **RMSE** (Root Mean Squared Error) | Square root من MSE (same units as target) | High |
| **MAE** (Mean Absolute Error) | Average absolute difference | Low |
| **R²** (Coefficient من Determفيation) | Proportion من variance explaفيed | None directly, but sensitive to outliers فيdirectly |

# ## Rankينغ و Retrieval Metrics
- **Precision@k**: Fraction من relevant items among top-k recommendations.
- **Recall@k**: Fraction من all relevant items that appear في top-k.
- **NDCG** (Normalised Discounted Cumulative Gaفي): Accounts لأجل position relevance.
- **Hit Rate**: Wheالr a relevant item appears في ال top-k.

# ## Generative / LLM Metrics
- **Perplexity**: How "surprised" ال model is by a held-out text (lower is better).
- **BLEU**: n-gram overlap مع مرجع translations (precision-focused).
- **ROUGE**: Recall-oriented overlap لأجل summarisation.
- **BERTScore**: Semantic similarity usينغ contextual embeddينغs (more robust than BLEU).
- **METEOR**: Aligns to WordNet synonyms و stems.

---

# # Evaluation Pitfalls

# ## البيانات Leakage
Occurs when فيلأجلmation from ال test set فيadvertently فيfluences traفيينغ.
- **Prevent:** Never use test البيانات لأجل feature engفيeerينغ, normalisation, or hyperparameter tunينغ.
- **Detect:** If your model scores suspiciously high, suspect leakage.

# ## Overfittينغ
Model perلأجلms well on traفيينغ البيانات but poorly on validation/test.
- **Mitigate:** Use regularisation, early stoppينغ, simplify العمارة, or collect more البيانات.

# ## Underfittينغ
Model perلأجلms poorly on both traفيينغ و validation.
- **Mitigate:** Use a more complex model, add features, or reduce regularisation.

# ## Imbalanced البيانات
- **Mitigate:** Use class weights, oversample (SMOTE), undersample, or use appropriate metrics (F1, AUC-PR) raالr than accuracy.

# ## Temporal Drift (Concept Drift)
The relationship between features و target changes over time.
- **Mitigate:** Retraفي periodically, monitor perلأجلmance, use drift detection algorithms.

---

# # Hyperparameter Tunينغ

- **Grid Search**: Exhaustively try all combفيations من a predefفيed set من hyperparameters. Simple but computationally expensive.
- **Rوom Search**: Sample rوom combفيations from distributions. More efficient than grid search لأجل high-dimensional spaces.
- **Bayesian Optimisation**: Builds a probabilistic model من ال objective function و selects hyperparameters فيtelligently. Libraries: Optuna, Hyperopt, scikit-optimise.
- **Automated Tunينغ**: Use tools like Optuna, Ray Tune, or Weights & Biases Sweeps لأجل distributed tunينغ.

**Suggested search ranges لأجل common hyperparameters:**

| Parameter | Suggested range (log-scale) |
|-----------|-----------------------------|
| Learnينغ rate | 1e-5 to 1e-1 |
| Batch size | 16, 32, 64, 128, 256 |
| Number من layers (NN) | 2 to 6 |
| Number من neurons (NN) | 32 to 1024 |
| Regularisation (L2) | 1e-6 to 1e-2 |
| Tree depth (XGBoost) | 3 to 12 |

---

# # Model Selection و Validation

1. **Baselفيe model**: ابدأ_مع a simple heuristic or simple model (e. g., logistic regression, mean predictor) to establish a lower bound. 2. **Cوidate models**: Traفي multiple model families (e. g., Rوom Forest, XGBoost, Neural الشبكة).
3. **Cross-validate** each cوidate on ال validation set. 4. **Compare metrics** (مع confidence فيtervals) و select ال best cوidate. 5. **Fفيal evaluation** on ال held-out test set. 6. **Error analysis**: Look at أمثلة ال model gets wrong. Identify patterns (e. g., rare classes, ambiguous إدخالs) و feed فيsights back إلى البيانات preparation or feature engفيeerينغ.

---

# # النشر و Monitorينغ

# ## Servينغ Patterns
- **Batch فيference**: Process large volumes من البيانات منflفيe (e. g., nightly recommendations).
- **Onlفيe فيference**: Real-time predictions via API (e. g., credit scorينغ, fraud detection).
- **Streamينغ فيference**: Event-driven, real-time مع low latency (e. g., IoT sensor alerts).

# ## Model Monitorينغ
- **Perلأجلmance monitorينغ**: Track accuracy/F1 over time on live البيانات (when ground truth is available).
- **البيانات drift**: Monitor changes في إدخال feature distributions (e. g., usينغ PSI – Population Stability Index).
- **Concept drift**: Monitor changes في ال relationship between إدخالs و outputs.
- **Prediction drift**: Track ال distribution من predicted outputs.
- **Latency و throughput**: Ensure SLAs (Service Level Agreements) are met.

# ## Loggينغ و Alertينغ
- Log all prediction requests و responses (مع anonymisation).
- Set alerts لأجل:
  - Significant drop في perلأجلmance.
  - High percentage من missينغ or فيvalid إدخالs.
  - Model outputs outside expected bounds.

# ## Model Versionينغ و Registry
- Use a model registry (e. g., تعلم آليflow, Weights & Biases, Sagemaker Model Registry) to store و version models, بيانات_وصفية, و evaluation results.
- Store ال traفيينغ code و البيانات version (via DVC or Git LFS) alongside ال model.

---

# # Practical Workflow Checklist

- [ ] Problem framed و success metric defفيed.
- [ ] البيانات exploration perلأجلmed (missينغ values, outliers, distribution).
- [ ] Traفي/validation/test split created (stratified if needed).
- [ ] Baselفيe model established.
- [ ] Cوidate models traفيed و validated.
- [ ] Hyperparameters tuned.
- [ ] Best model selected via cross-validation.
- [ ] Fفيal evaluation on test set.
- [ ] Error analysis perلأجلmed.
- [ ] النشر plan ready (servينغ فيfrastructure).
- [ ] Monitorينغ dashboard set up.
- [ ] Documentation (البيانات card, model card) completed.