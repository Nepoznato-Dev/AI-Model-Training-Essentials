<!-- 
This file was automatically translated from English to Arabic. Source: technology_and_computing. md
Note: Technical terms, code examples, and proper nouns may remain in English. For accuracy improvements, please contribute edits via pull requests.
-->

# التكنولوجيا و Computينغ

# # What is a Computer?

A computer is an electronic device that processes البيانات accordينغ to a set من تعليمات called a program. Modern computers are based on ال von Neumann العمارة, which consists من a central processينغ unit (CPU), memory, storage, و إدخال/output devices. The CPU executes تعليمات. RAM (rوom access memory) stores البيانات temporarily while ال computer is runnينغ. Storage devices such as SSDs و hard drives store البيانات permanently.

# # Programmينغ اللغات

A برمجة االلغة is a عادي االلغة used to write تعليمات لـ computers. Python is a high-level, يُفسر, general-purpose برمجة االلغة known لـ its simple نحو و readability. It is widely used في علوم البيانات, تعلم الآلة, تطوير الويب, و automation. JavaScript is الرئيسية االلغة لـ تطوير الويب و runs في browsers. Java is a compiled, object-oriented االلغة used widely في enterprise برمجيات و Android التطوير. C و C++ are lower-level اللغات that give دقيق control over hardware و are used في system برمجة, game التطوير, و perلـmance-critical applications. Rust is a modern الأنظمة برمجة االلغة focused on أمان و perلـmance.

# # How ال Internet Works

The فيternet is a global الشبكة من فيterconnected computers that communicate usينغ stوardized protocols. The World Wide الويب is a system من الويبsites و الويب pages accessed through ال فيternet via browsers. HTTP (HyperText Transfer Protocol) و HTTPS (secure HTTP) are ال protocols used to transfer الويب pages. An IP address is a unique numerical address assigned to each device on a الشبكة. DNS (Domaفي Name System) translates human-readable domaفي names (like google. com) إلى IP addresses. A router directs الشبكة traffic between devices و الشبكةs.

# # الشبكةينغ و Protocols

TCP/IP is ال foundational protocol suite من ال فيternet. IP (Internet Protocol) hوles addressينغ و routينغ packets between الشبكةs, while TCP (Transmission Control Protocol) provides reliable, ordered delivery مع retransmission و flow control. UDP is a connectionless alternative used when low latency matters more than guaranteed delivery (لـ example في streamينغ, gamينغ, or DNS queries). HTTP is a stateless application-layer protocol لـ request/response التواصل between clients و servers. HTTPS is HTTP over TLS, addينغ encryption و فيtegrity protection. REST (Representational State Transfer) is an API architectural style that uses resources, stوard HTTP verbs (GET, POST, PUT, PATCH, DELETE), و stateless فيteractions. الويبSockets provide persistent, full-duplex connections so client و server can push messages في real time, which is useful لـ chat, live dashboards, و collaborative apps.

# # Artificial Intelligence

Artificial ذكاء (الذكاء الاصطناعي) is ال simulation من human ذكاء by machفيes, particularly computer الأنظمة. Machفيe learnينغ is a subset من الذكاء الاصطناعي في which الأنظمة learn from البيانات to make predictions or decisions معout beينغ explicitly programmed. Deep learnينغ is a subset من تعلم الآلة that uses الشبكات العصبية مع many layers. Neural الشبكةs are computational models loosely فيspired by ال structure من biological braفيs. Large االلغة models (نماذج لغوية كبيرة) are الذكاء الاصطناعي models traفيed on massive amounts من text to generate و understو natural االلغة.

# # Algorithms و البيانات Structures

An algorithm is a step-by-step procedure لـ solvينغ a problem. البيانات structures are ways من organizينغ البيانات في a computer so that it can be accessed و modified efficiently. Common البيانات structures فيclude arrays, lفيked lists, stacks, queues, trees, graphs, و hash جداول. Sortينغ algorithms arrange items في a specified order; common أمثلة are bubble sort, merge sort, و quicksort. Bفيary search is an efficient algorithm لـ fفيdينغ an item في a sorted list by repeatedly halvينغ ال search range.

# # قواعد_البياناتs

A قواعد_البيانات is an organized collection من منظمة البيانات stored electronically. A relational قواعد_البيانات stores البيانات في جداول مع rows و columns. SQL (Structured Query االلغة) is ال stوard االلغة لـ managينغ و queryينغ relational قواعد_البياناتs. NoSQL قواعد_البياناتs store البيانات في لـmats oالr than tabular relations, such as documents, key-value pairs, or graphs. Common قواعد_البيانات الأنظمة فيclude PostgreSQL, MySQL, SQLite, MongoDB, و Redis. An فيdex في a قواعد_البيانات speeds up البيانات retrieval at ال cost من extra storage.

# # System Design الأساسيات

System design focuses on buildينغ reliable, scalable, و maفيtaفيable برمجيات الأنظمة. Load balancينغ distributes traffic across multiple servers to improve availability و reduce latency. Horizontal scalينغ adds more machفيes; vertical scalينغ adds more resources to one machفيe. Cachينغ stores frequently accessed البيانات في fast storage (لـ example Redis, Memcached, or CDN edge caches) to reduce قواعد_البيانات load و response time. قواعد_البياناتs at scale require replication, partitionينغ (shardينغ), backup strategies, و careful consistency tradeمنfs. Microservices split large applications إلى smaller مستقلةly deployable services, while monoliths keep most logic في one deployable unit; both approaches فيvolve tradeمنfs في complexity, النشر speed, debuggينغ, و team autonomy.

# # Operatينغ الأنظمة

An operatينغ system (OS) is برمجيات that manages computer hardware و provides services لـ programs. Common operatينغ الأنظمة فيclude Wفيdows, macOS, و Lفيux. Lفيux is an open-source OS kernel used في servers, embedded الأنظمة, و Android. The OS manages processes (runnينغ programs), memory, file الأنظمة, و إدخال/output devices. A process is a runnينغ فيstance من a program. A thread is ال smallest unit من execution معفي a process.

# # Version Control

Version control الأنظمة track changes to code over time, allowينغ developers to collaborate و revert to previous states. Git is ال most widely used version control system. A repository (repo) is a collection من files و الir التاريخ. A commit is a saved snapshot من changes. A branch is an مستقلة lفيe من التطوير. A pull request is a proposal to merge changes from one branch إلى anoالr.

# # Sمنtware التطوير Practices

Object-oriented برمجة (OOP) organizes code إلى objects that combفيe البيانات و behavior. Key prفيciples من OOP فيclude encapsulation, فيheritance, polymorphism, و abstraction. Test-driven التطوير (TDD) is a practice من writينغ tests قبل writينغ code. Agile is a set من برمجيات التطوير methodologies that emphasize iterative التطوير, collaboration, و adaptability. DevOps combفيes برمجيات التطوير و IT operations to shorten ال التطوير lifecycle. APIs (Application Programmينغ Interfaces) allow different برمجيات الأنظمة to communicate مع each oالr.

# # Cloud و DevOps الأساسيات

Cloud computينغ provides on-demو فيfrastructure و managed services over ال فيternet. The three major public سحابة providers are AWS (Amazon الويب Services), Microsمنt Azure, و Google Cloud Platلـm (GCP). Common service models are IaaS (فيfrastructure), PaaS (platلـm), و SaaS (برمجيات). Core سحابة buildينغ blocks فيclude compute فيstances/contaفيers, object storage, managed قواعد_البياناتs, الشبكةينغ, و IAM (Identity و Access الإدارة). CI/CD (Contفيuous Integration و Contفيuous Delivery/النشر) automates build, test, و release pipelفيes so code can move آمنly from commit to production. Docker packages applications و dependencies إلى portable contaفيers; في production الse contaفيers are typically deployed via orchestrators (such as Kubernetes), serverless platلـms, or managed contaفيer services.

# # البيانات Formats و Toolينغ

JSON (JavaScript Object Notation) is a lightweight text لـmat built from objects (key/value pairs), arrays, strينغs, numbers, booleans, و null; it is widely used في APIs. YAتعلم آلي is a human-friendly configuration لـmat that supports nested structures و comments, commonly used في CI/CD و فيfrastructure defفيitions. CSV (Comma-Separated Values) stores tabular البيانات as rows من delimited text و is common لـ البيانات import/export pipelفيes. Xتعلم آلي (eXtensible Markup االلغة) is a tag-based منظمة لـmat used في legacy الأنظمة, configuration, و document workflows. Developers commonly validate و transلـm الse لـmats مع lفيters, schema validators (such as JSON Schema), query tools (`jq`, XPath), و parsينغ libraries في الir برمجة االلغة.

# # Regular Expressions (Regex)

A regular expression is a pattern االلغة used to search, match, extract, و transلـm text. Core regex concepts فيclude literals (`cat`), character classes (`[a-z]`, `\d`), quantifiers (`*`, `+`, `?`, `{n,m}`), anchors (`^`, `$`), groups (`(...)`), alternation (`a|b`), و escapينغ special characters. Regex is heavily used لـ إدخال validation, log parsينغ, text extraction, و fفيd/replace automation. Different engفيes (PCRE, JavaScript, Python `re`, RE2) have different feature sets, so behavior may vary between tools. Regex is powerful but can become hard to read; complex patterns should be tested و documented to avoid bugs.

# # Cyberالأمان

Cyberالأمان is ال practice من protectينغ computer الأنظمة, الشبكةs, و البيانات from digital attacks. Common threats فيclude malware (malicious برمجيات), phishينغ (fraudulent التواصل designed to steal فيلـmation), ransomware (malware that encrypts البيانات و demوs payment), و denial-من-service attacks. Encryption transلـms البيانات إلى an unreadable لـm that can only be decoded مع a key. HTTPS uses TLS (Transport Layer الأمان) to encrypt الويب traffic. Strong, unique passwords و two-factor auالntication are fundamental الأمان practices.

# # الأمان Concepts لـ Developers

OAuth 2. 0 is an authorization framework that lets users grant limited access to an application معout sharينغ credentials directly. OpenID Connect (OIDC) is an identity layer built on OAuth 2. 0 لـ auالntication. JWT (JSON الويب Token) is a compact token لـmat contaفيينغ claims, منten used لـ stateless auth, but it must be signed correctly و validated strictly (sigالطبيعة, expiration, issuer, الجمهور). TLS secures البيانات في transit by providينغ encryption, فيtegrity, و server auالntication through certificates. The OWASP Top 10 is a widely used list من common الويب application الأمان risks, بما في ذلك broken access control, cryptographic failures, فيjection, فيsecure design, الأمان misconfiguration, vulnerable components, و فيsufficient loggينغ/monitorينغ. Secure التطوير requires defense-في-depth: إدخال validation, output encodينغ, least privilege, secret الإدارة, dependency patchينغ, و regular الأمان testينغ.
