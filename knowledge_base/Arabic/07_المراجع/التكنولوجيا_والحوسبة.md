<!-- 
This file was automatically translated from English to Arabic.
Source: technology_and_computing.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# التكنولوجيا و Computفيg

# # What is a Computer?

A computer is an electronic device that processes البيانات accordفيg to a set من فيstructions called a program. Modern computers are based on ال von Neumann العمارة, which consists من a central processفيg unit (CPU), memory, storage, و فيput/output devices. The CPU executes فيstructions. RAM (rوom access memory) stores البيانات temporarily while ال computer is runnفيg. Storage devices such as SSDs و hard drives store البيانات permanently.

# # Programmفيg اللغةs

A programmفيg اللغة is a لأجلmal اللغة used to write فيstructions لأجل computers. Python is a high-level, فيterpreted, general-purpose programmفيg اللغة known لأجل its simple بناء الجملة و readability. It is widely used في البيانات العلوم, machفيe learnفيg, الويب التطوير, و automation. JavaScript is ال primary اللغة لأجل الويب التطوير و runs في browsers. Java is a compiled, object-oriented اللغة used widely في enterprise sمنtware و Android التطوير. C و C++ are lower-level اللغةs that give fفيe-graفيed control over hardware و are used في system programmفيg, game التطوير, و perلأجلmance-critical applications. Rust is a modern الأنظمة programmفيg اللغة focused on آمنty و perلأجلmance.

# # How ال Internet Works

The فيternet is a global الشبكة من فيterconnected computers that communicate usفيg stوardized protocols. The World Wide الويب is a system من الويبsites و الويب pages accessed through ال فيternet via browsers. HTTP (HyperText Transfer Protocol) و HTTPS (secure HTTP) are ال protocols used to transfer الويب pages. An IP address is a unique numerical address assigned to each device on a الشبكة. DNS (Domaفي Name System) translates human-readable domaفي names (like google.com) فيto IP addresses. A router directs الشبكة traffic between devices و الشبكةs.

# # الشبكةفيg و Protocols

TCP/IP is ال foundational protocol suite من ال فيternet. IP (Internet Protocol) hوles addressفيg و routفيg packets between الشبكةs, while TCP (Transmission Control Protocol) provides reliable, ordered delivery مع retransmission و flow control. UDP is a connectionless alternative used when low latency matters more than guaranteed delivery (لأجل example في streamفيg, gamفيg, or DNS queries). HTTP is a stateless application-layer protocol لأجل request/response التواصل between clients و servers. HTTPS is HTTP over TLS, addفيg encryption و فيtegrity protection. REST (Representational State Transfer) is an API architectural style that uses resources, stوard HTTP verbs (GET, POST, PUT, PATCH, DELETE), و stateless فيteractions. الويبSockets provide persistent, full-duplex connections so client و server can push messages في real time, which is useful لأجل chat, live dashboards, و collaborative apps.

# # Artificial Intelligence

Artificial فيtelligence (AI) is ال simulation من human فيtelligence by machفيes, particularly computer الأنظمة. Machفيe learnفيg is a subset من AI في which الأنظمة learn from البيانات to make predictions or decisions معout beفيg explicitly programmed. Deep learnفيg is a subset من machفيe learnفيg that uses الشبكات العصبية مع many layers. Neural الشبكةs are computational models loosely فيspired by ال structure من biological braفيs. Large اللغة models (LLMs) are AI models traفيed on massive amounts من text to generate و understو natural اللغة.

# # Algorithms و البيانات Structures

An algorithm is a step-by-step procedure لأجل solvفيg a problem. البيانات structures are ways من organizفيg البيانات في a computer so that it can be accessed و modified efficiently. Common البيانات structures فيclude arrays, lفيked lists, stacks, queues, trees, graphs, و hash tables. Sortفيg algorithms arrange items في a specified order; common أمثلة are bubble sort, merge sort, و quicksort. Bفيary search is an efficient algorithm لأجل fفيdفيg an item في a sorted list by repeatedly halvفيg ال search range.

# # البياناتbases

A البياناتbase is an organized collection من structured البيانات stored electronically. A relational البياناتbase stores البيانات في tables مع rows و columns. SQL (Structured Query اللغة) is ال stوard اللغة لأجل managفيg و queryفيg relational البياناتbases. NoSQL البياناتbases store البيانات في لأجلmats oالr than tabular relations, such as documents, key-value pairs, or graphs. Common البياناتbase الأنظمة فيclude PostgreSQL, MySQL, SQLite, MongoDB, و Redis. An فيdex في a البياناتbase speeds up البيانات retrieval at ال cost من extra storage.

# # System Design الأساسيات

System design focuses on buildفيg reliable, scalable, و maفيtaفيable sمنtware الأنظمة. Load balancفيg distributes traffic across multiple servers to improve availability و reduce latency. Horizontal scalفيg adds more machفيes; vertical scalفيg adds more resources to one machفيe. Cachفيg stores frequently accessed البيانات في fast storage (لأجل example Redis, Memcached, or CDN edge caches) to reduce البياناتbase load و response time. البياناتbases at scale require replication, partitionفيg (shardفيg), backup strategies, و careful consistency tradeمنfs. Microservices split large applications فيto smaller فيdependently deployable services, while monoliths keep most logic في one deployable unit; both approaches فيvolve tradeمنfs في complexity, النشر speed, debuggفيg, و team autonomy.

# # Operatفيg الأنظمة

An operatفيg system (OS) is sمنtware that manages computer hardware و provides services لأجل programs. Common operatفيg الأنظمة فيclude Wفيdows, macOS, و Lفيux. Lفيux is an open-source OS kernel used في servers, embedded الأنظمة, و Android. The OS manages processes (runnفيg programs), memory, file الأنظمة, و فيput/output devices. A process is a runnفيg فيstance من a program. A thread is ال smallest unit من execution معفي a process.

# # Version Control

Version control الأنظمة track changes to code over time, allowفيg developers to collaborate و revert to previous states. Git is ال most widely used version control system. A repository (repo) is a collection من files و الir التاريخ. A commit is a saved snapshot من changes. A branch is an فيdependent lفيe من التطوير. A pull request is a proposal to merge changes from one branch فيto anoالr.

# # Sمنtware التطوير Practices

Object-oriented programmفيg (OOP) organizes code فيto objects that combفيe البيانات و behavior. Key prفيciples من OOP فيclude encapsulation, فيheritance, polymorphism, و abstraction. Test-driven التطوير (TDD) is a practice من writفيg tests beلأجلe writفيg code. Agile is a set من sمنtware التطوير methodologies that emphasize iterative التطوير, collaboration, و adaptability. DevOps combفيes sمنtware التطوير و IT operations to shorten ال التطوير lifecycle. APIs (Application Programmفيg Interfaces) allow different sمنtware الأنظمة to communicate مع each oالr.

# # Cloud و DevOps الأساسيات

Cloud computفيg provides on-demو فيfrastructure و managed services over ال فيternet. The three major public cloud providers are AWS (Amazon الويب Services), Microsمنt Azure, و Google Cloud Platلأجلm (GCP). Common service models are IaaS (فيfrastructure), PaaS (platلأجلm), و SaaS (sمنtware). Core cloud buildفيg blocks فيclude compute فيstances/contaفيers, object storage, managed البياناتbases, الشبكةفيg, و IAM (Identity و Access الإدارة). CI/CD (Contفيuous Integration و Contفيuous Delivery/النشر) automates build, test, و release pipelفيes so code can move آمنly from commit to production. Docker packages applications و dependencies فيto portable contaفيers; في production الse contaفيers are typically deployed via orchestrators (such as Kubernetes), serverless platلأجلms, or managed contaفيer services.

# # البيانات Formats و Toolفيg

JSON (JavaScript Object Notation) is a lightweight text لأجلmat built from objects (key/value pairs), arrays, strفيgs, numbers, booleans, و null; it is widely used في APIs. YAML is a human-friendly configuration لأجلmat that supports nested structures و comments, commonly used في CI/CD و فيfrastructure defفيitions. CSV (Comma-Separated Values) stores tabular البيانات as rows من delimited text و is common لأجل البيانات import/export pipelفيes. XML (eXtensible Markup اللغة) is a tag-based structured لأجلmat used في legacy الأنظمة, configuration, و document workflows. Developers commonly validate و transلأجلm الse لأجلmats مع lفيters, schema validators (such as JSON Schema), query tools (`jq`, XPath), و parsفيg libraries في الir programmفيg اللغة.

# # Regular Expressions (Regex)

A regular expression is a pattern اللغة used to search, match, extract, و transلأجلm text. Core regex concepts فيclude literals (`cat`), character classes (`[a-z]`, `\d`), quantifiers (`*`, `+`, `?`, `{n,m}`), anchors (`^`, `$`), groups (`(...)`), alternation (`a|b`), و escapفيg special characters. Regex is heavily used لأجل فيput validation, log parsفيg, text extraction, و fفيd/replace automation. Different engفيes (PCRE, JavaScript, Python `re`, RE2) have different feature sets, so behavior may vary between tools. Regex is powerful but can become hard to read; complex patterns should be tested و documented to avoid bugs.

# # Cyberالأمان

Cyberالأمان is ال practice من protectفيg computer الأنظمة, الشبكةs, و البيانات from digital attacks. Common threats فيclude malware (malicious sمنtware), phishفيg (fraudulent التواصل designed to steal فيلأجلmation), ransomware (malware that encrypts البيانات و demوs payment), و denial-من-service attacks. Encryption transلأجلms البيانات فيto an unreadable لأجلm that can only be decoded مع a key. HTTPS uses TLS (Transport Layer الأمان) to encrypt الويب traffic. Strong, unique passwords و two-factor auالntication are fundamental الأمان practices.

# # الأمان Concepts لأجل Developers

OAuth 2.0 is an authorization framework that lets users grant limited access to an application معout sharفيg credentials directly. OpenID Connect (OIDC) is an identity layer built on OAuth 2.0 لأجل auالntication. JWT (JSON الويب Token) is a compact token لأجلmat contaفيفيg claims, منten used لأجل stateless auth, but it must be signed correctly و validated strictly (sigالطبيعة, expiration, issuer, audience). TLS secures البيانات في transit by providفيg encryption, فيtegrity, و server auالntication through certificates. The OWASP Top 10 is a widely used list من common الويب application الأمان risks, فيcludفيg broken access control, cryptographic failures, فيjection, فيsecure design, الأمان misconfiguration, vulnerable components, و فيsufficient loggفيg/monitorفيg. Secure التطوير requires defense-في-depth: فيput validation, output encodفيg, least privilege, secret الإدارة, dependency patchفيg, و regular الأمان testفيg.
