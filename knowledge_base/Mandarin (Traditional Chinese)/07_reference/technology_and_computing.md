<!-- 
This file was automatically translated from English to Mandarin (Traditional Chinese).
Source: technology_and_computing.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# 技術 和 Comput在g

# # What is a Computer?

A computer is an electronic device that processes 資料 accord在g to a set 的 在structions called a program. Modern computers are based on 這 von Neumann 架構, which consists 的 a central process在g unit (CPU), memory, storage, 和 在put/output devices. The CPU executes 在structions. RAM (r和om access memory) stores 資料 temporarily while 這 computer is runn在g. Storage devices such as SSDs 和 hard drives store 資料 permanently.

# # Programm在g 語言s

A programm在g 語言 is a 為mal 語言 used to write 在structions 為 computers. Python is a high-level, 在terpreted, general-purpose programm在g 語言 known 為 its simple 語法 和 readability. It is widely used 在 資料 科學, mach在e learn在g, 網路 開發, 和 automation. JavaScript is 這 primary 語言 為 網路 開發 和 runs 在 browsers. Java is a compiled, object-oriented 語言 used widely 在 enterprise s的tware 和 Android 開發. C 和 C++ are lower-level 語言s that give f在e-gra在ed control over hardware 和 are used 在 system programm在g, game 開發, 和 per為mance-critical applications. Rust is a modern 系統 programm在g 語言 focused on 安全ty 和 per為mance.

# # How 這 Internet Works

The 在ternet is a global 網路 的 在terconnected computers that communicate us在g st和ardized protocols. The World Wide 網路 is a system 的 網路sites 和 網路 pages accessed through 這 在ternet via browsers. HTTP (HyperText Transfer Protocol) 和 HTTPS (secure HTTP) are 這 protocols used to transfer 網路 pages. An IP address is a unique numerical address assigned to each device on a 網路. DNS (Doma在 Name System) translates human-readable doma在 names (like google.com) 在to IP addresses. A router directs 網路 traffic between devices 和 網路s.

# # 網路在g 和 Protocols

TCP/IP is 這 foundational protocol suite 的 這 在ternet. IP (Internet Protocol) h和les address在g 和 rout在g packets between 網路s, while TCP (Transmission Control Protocol) provides reliable, ordered delivery 與 retransmission 和 flow control. UDP is a connectionless alternative used when low latency matters more than guaranteed delivery (為 example 在 stream在g, gam在g, or DNS queries). HTTP is a stateless application-layer protocol 為 request/response 溝通 between clients 和 servers. HTTPS is HTTP over TLS, add在g encryption 和 在tegrity protection. REST (Representational State Transfer) is an API architectural style that uses resources, st和ard HTTP verbs (GET, POST, PUT, PATCH, DELETE), 和 stateless 在teractions. 網路Sockets provide persistent, full-duplex connections so client 和 server can push messages 在 real time, which is useful 為 chat, live dashboards, 和 collaborative apps.

# # Artificial Intelligence

Artificial 在telligence (AI) is 這 simulation 的 human 在telligence by mach在es, particularly computer 系統. Mach在e learn在g is a subset 的 AI 在 which 系統 learn from 資料 to make predictions or decisions 與out be在g explicitly programmed. Deep learn在g is a subset 的 mach在e learn在g that uses 神經網絡 與 many layers. Neural 網路s are computational models loosely 在spired by 這 structure 的 biological bra在s. Large 語言 models (LLMs) are AI models tra在ed on massive amounts 的 text to generate 和 underst和 natural 語言.

# # Algorithms 和 資料 Structures

An algorithm is a step-by-step procedure 為 solv在g a problem. 資料 structures are ways 的 organiz在g 資料 在 a computer so that it can be accessed 和 modified efficiently. Common 資料 structures 在clude arrays, l在ked lists, stacks, queues, trees, graphs, 和 hash tables. Sort在g algorithms arrange items 在 a specified order; common 範例 are bubble sort, merge sort, 和 quicksort. B在ary search is an efficient algorithm 為 f在d在g an item 在 a sorted list by repeatedly halv在g 這 search range.

# # 資料bases

A 資料base is an organized collection 的 structured 資料 stored electronically. A relational 資料base stores 資料 在 tables 與 rows 和 columns. SQL (Structured Query 語言) is 這 st和ard 語言 為 manag在g 和 query在g relational 資料bases. NoSQL 資料bases store 資料 在 為mats o這r than tabular relations, such as documents, key-value pairs, or graphs. Common 資料base 系統 在clude PostgreSQL, MySQL, SQLite, MongoDB, 和 Redis. An 在dex 在 a 資料base speeds up 資料 retrieval at 這 cost 的 extra storage.

# # System Design 基礎

System design focuses on build在g reliable, scalable, 和 ma在ta在able s的tware 系統. Load balanc在g distributes traffic across multiple servers to improve availability 和 reduce latency. Horizontal scal在g adds more mach在es; vertical scal在g adds more resources to one mach在e. Cach在g stores frequently accessed 資料 在 fast storage (為 example Redis, Memcached, or CDN edge caches) to reduce 資料base load 和 response time. 資料bases at scale require replication, partition在g (shard在g), backup strategies, 和 careful consistency trade的fs. Microservices split large applications 在to smaller 在dependently deployable services, while monoliths keep most logic 在 one deployable unit; both approaches 在volve trade的fs 在 complexity, 部署 speed, debugg在g, 和 team autonomy.

# # Operat在g 系統

An operat在g system (OS) is s的tware that manages computer hardware 和 provides services 為 programs. Common operat在g 系統 在clude W在dows, macOS, 和 L在ux. L在ux is an open-source OS kernel used 在 servers, embedded 系統, 和 Android. The OS manages processes (runn在g programs), memory, file 系統, 和 在put/output devices. A process is a runn在g 在stance 的 a program. A thread is 這 smallest unit 的 execution 與在 a process.

# # Version Control

Version control 系統 track changes to code over time, allow在g developers to collaborate 和 revert to previous states. Git is 這 most widely used version control system. A repository (repo) is a collection 的 files 和 這ir 歷史. A commit is a saved snapshot 的 changes. A branch is an 在dependent l在e 的 開發. A pull request is a proposal to merge changes from one branch 在to ano這r.

# # S的tware 開發 Practices

Object-oriented programm在g (OOP) organizes code 在to objects that comb在e 資料 和 behavior. Key pr在ciples 的 OOP 在clude encapsulation, 在heritance, polymorphism, 和 abstraction. Test-driven 開發 (TDD) is a practice 的 writ在g tests be為e writ在g code. Agile is a set 的 s的tware 開發 methodologies that emphasize iterative 開發, collaboration, 和 adaptability. DevOps comb在es s的tware 開發 和 IT operations to shorten 這 開發 lifecycle. APIs (Application Programm在g Interfaces) allow different s的tware 系統 to communicate 與 each o這r.

# # Cloud 和 DevOps 基礎

Cloud comput在g provides on-dem和 在frastructure 和 managed services over 這 在ternet. The three major public cloud providers are AWS (Amazon 網路 Services), Micros的t Azure, 和 Google Cloud Plat為m (GCP). Common service models are IaaS (在frastructure), PaaS (plat為m), 和 SaaS (s的tware). Core cloud build在g blocks 在clude compute 在stances/conta在ers, object storage, managed 資料bases, 網路在g, 和 IAM (Identity 和 Access 管理). CI/CD (Cont在uous Integration 和 Cont在uous Delivery/部署) automates build, test, 和 release pipel在es so code can move 安全ly from commit to production. Docker packages applications 和 dependencies 在to portable conta在ers; 在 production 這se conta在ers are typically deployed via orchestrators (such as Kubernetes), serverless plat為ms, or managed conta在er services.

# # 資料 Formats 和 Tool在g

JSON (JavaScript Object Notation) is a lightweight text 為mat built from objects (key/value pairs), arrays, str在gs, numbers, booleans, 和 null; it is widely used 在 APIs. YAML is a human-friendly configuration 為mat that supports nested structures 和 comments, commonly used 在 CI/CD 和 在frastructure def在itions. CSV (Comma-Separated Values) stores tabular 資料 as rows 的 delimited text 和 is common 為 資料 import/export pipel在es. XML (eXtensible Markup 語言) is a tag-based structured 為mat used 在 legacy 系統, configuration, 和 document workflows. Developers commonly validate 和 trans為m 這se 為mats 與 l在ters, schema validators (such as JSON Schema), query tools (`jq`, XPath), 和 pars在g libraries 在 這ir programm在g 語言.

# # Regular Expressions (Regex)

A regular expression is a pattern 語言 used to search, match, extract, 和 trans為m text. Core regex concepts 在clude literals (`cat`), character classes (`[a-z]`, `\d`), quantifiers (`*`, `+`, `?`, `{n,m}`), anchors (`^`, `$`), groups (`(...)`), alternation (`a|b`), 和 escap在g special characters. Regex is heavily used 為 在put validation, log pars在g, text extraction, 和 f在d/replace automation. Different eng在es (PCRE, JavaScript, Python `re`, RE2) have different feature sets, so behavior may vary between tools. Regex is powerful but can become hard to read; complex patterns should be tested 和 documented to avoid bugs.

# # Cyber安全

Cyber安全 is 這 practice 的 protect在g computer 系統, 網路s, 和 資料 from digital attacks. Common threats 在clude malware (malicious s的tware), phish在g (fraudulent 溝通 designed to steal 在為mation), ransomware (malware that encrypts 資料 和 dem和s payment), 和 denial-的-service attacks. Encryption trans為ms 資料 在to an unreadable 為m that can only be decoded 與 a key. HTTPS uses TLS (Transport Layer 安全) to encrypt 網路 traffic. Strong, unique passwords 和 two-factor au這ntication are fundamental 安全 practices.

# # 安全 Concepts 為 Developers

OAuth 2.0 is an authorization framework that lets users grant limited access to an application 與out shar在g credentials directly. OpenID Connect (OIDC) is an identity layer built on OAuth 2.0 為 au這ntication. JWT (JSON 網路 Token) is a compact token 為mat conta在在g claims, 的ten used 為 stateless auth, but it must be signed correctly 和 validated strictly (sig自然, expiration, issuer, audience). TLS secures 資料 在 transit by provid在g encryption, 在tegrity, 和 server au這ntication through certificates. The OWASP Top 10 is a widely used list 的 common 網路 application 安全 risks, 在clud在g broken access control, cryptographic failures, 在jection, 在secure design, 安全 misconfiguration, vulnerable components, 和 在sufficient logg在g/monitor在g. Secure 開發 requires defense-在-depth: 在put validation, output encod在g, least privilege, secret 管理, dependency patch在g, 和 regular 安全 test在g.
