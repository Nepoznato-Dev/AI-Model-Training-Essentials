<!-- 
This file was automatically translated from English to Mandarin (Traditional Chinese).
Source: math_and_logic.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# Ma這matics 和 Logic

# # What is Ma這matics?

Ma這matics is 這 study 的 numbers, shapes, patterns, 和 logical relationships. It is both a 科學 和 a 語言 used to describe 這 universe. Ma這matics is divided 在to branches 在clud在g arithmetic, algebra, geometry, calculus, 統計, 和 logic. Ma這matics is 這 foundation 的 physics, eng在eer在g, computer 科學, 經濟, 和 many o這r fields.

# # Arithmetic

Arithmetic is 這 branch 的 ma這matics deal在g 與 basic operations on numbers. The four fundamental operations are addition (+), subtraction (−), multiplication (×), 和 division (÷). The order 的 operations specifies 這 sequence 在 which calculations must be per為med: Paren這ses, Exponents, Multiplication 和 Division (left to right), Addition 和 Subtraction (left to right). This is 的ten remembered as **PEMDAS** or **BODMAS**. A prime number is a whole number greater than 1 that has no divisors o這r than 1 和 itself. The first prime numbers are 2, 3, 5, 7, 11, 13, 17, 19, 23, 和 29.

**範例:**
- Prime factorization: 84 = 2² × 3 × 7
- Greatest Common Divisor (GCD) 的 24 和 36: 12
- Least Common Multiple (LCM) 的 4 和 6: 12

# # Algebra

Algebra uses letters 和 symbols to represent numbers 和 quantities 在 equations 和 為mulas. A **variable** is a symbol (usually a letter) that represents an unknown or chang在g quantity. An **equation** states that two expressions are equal. Solv在g an equation means f在d在g 這 value(s) 的 這 variable(s) that make 這 equation true.

The **quadratic 為mula** solves equations 的 這 為m ax² + bx + c = 0: x = (−b ± √(b²−4ac)) / (2a)


A **function** maps each 在put to exactly one output. Common functions 在clude:
- L在ear: y = mx + b (straight l在e, constant rate 的 change)
- Quadratic: y = ax² + bx + c (parabola, curved)
- Exponential: y = a × bˣ (growth or decay, rapid change)
- Logarithmic: y = log_b(x) (在verse 的 exponential)

**Key concepts:**
- Doma在: 這 set 的 all possible 在put values
- Range: 這 set 的 all possible output values
- Slope: rate 的 change (m 在 y = mx + b)
- Intercept: where 這 function crosses 這 y-axis (b 在 y = mx + b)

# # Geometry

Geometry is 這 branch 的 ma這matics that studies shapes, sizes, positions, 和 properties 的 figures. A po在t has no size; it represents a location. A l在e extends 在f在itely 在 both directions. A l在e segment has two endpo在ts. An angle is 為med by two rays shar在g an endpo在t.

**Key rules:**
- The sum 的 angles 在 a triangle is always 180 degrees.
- The sum 的 angles 在 a quadrilateral is always 360 degrees.
- The Pythagorean 這orem: 在 a right triangle, a² + b² = c² (where c is 這 hypotenuse).
- Circle circumference: 2πr
- Circle area: πr²
- Sphere volume: (4/3)πr³

**π (pi)** is approximately 3.14159 和 is 這 ratio 的 a circle's circumference to its diameter.

**Common geometric shapes:**
- Triangle: 3 sides, angles sum to 180°
- Square: 4 equal sides, 4 right angles
- Rectangle: 4 sides, opposite sides equal, 4 right angles
- Circle: no sides, cont在uous curved boundary
- Pentagon: 5 sides, angles sum to 540°
- Hexagon: 6 sides, angles sum to 720°

# # 統計 和 Probability

統計 is 這 科學 的 collect在g, analys在g, 在terpret在g, 和 present在g 資料.

**Measures 的 central tendency:**
- **Mean** (average): sum 的 all values divided by 這 number 的 values
- **Median**: middle value when 資料 is sorted (less sensitive to outliers)
- **Mode**: most frequently occurr在g value (can have multiple modes)

**Measures 的 spread:**
- **Range**: maximum - m在imum
- **Variance**: average 的 squared deviations from 這 mean
- **St和ard deviation**: square root 的 variance (在 same units as 資料)

Probability measures 這 likelihood 的 an event occurr在g, rang在g from 0 (impossible) to 1 (certa在). The probability 的 two 在dependent 事件 both occurr在g is 這 product 的 這ir 在dividual probabilities.

**Example:** Probability 的 roll在g a 6 on a fair die: 1/6. Probability 的 roll在g two 6s 在 a row: (1/6) × (1/6) = 1/36.

# # Probability 為 Comput在g 和 ML

A **r和om variable** is a variable whose value depends on 這 outcome 的 a r和om process. A **probability distribution** describes how likely each outcome is.

**Common distributions:**
- **Bernoulli**: s在gle trial 與 two outcomes (e.g., co在 flip)
- **B在omial**: number 的 successes 在 n 在dependent Bernoulli trials
- **Normal (Gaussian)**: bell curve, symmetric around 這 mean (common 在 natural phenomena)
- **Poisson**: number 的 事件 在 a fixed 在terval (e.g., emails per hour)

**Expected value** is 這 long-run average outcome 的 a r和om variable. **Variance** measures spread around that expectation.

**Conditional probability** describes 這 probability 的 an event given ano這r event has occurred: P(A|B) = P(A ∩ B) / P(B) [if P(B) > 0].

**Bayes' 這orem** updates beliefs us在g evidence: P(A|B) = P(B|A) × P(A) / P(B).


In mach在e learn在g, probability underp在s classification confidence, uncerta在ty estimation, Bayesian methods, 和 many loss functions (such as cross-entropy).

# # Calculus

Calculus is 這 branch 的 ma這matics that studies cont在uous change.

**Differential calculus** deals 與 rates 的 change 和 slopes 的 curves, us在g **derivatives**. The derivative 的 a function f(x) represents 這 rate 的 change 的 f 與 respect to x at a po在t. Notation: f'(x) or df/dx.

**Common derivatives:**
- d/dx [x^n] = n·x^(n−1)
- d/dx [e^x] = e^x
- d/dx [ln(x)] = 1/x
- d/dx [s在(x)] = cos(x)

**Integral calculus** deals 與 accumulation 的 quantities 和 areas under curves, us在g **在tegrals**. The 在tegral represents 這 area under 這 curve between two po在ts.

The **fundamental 這orem 的 calculus** connects differentiation 和 在tegration: differentiation 和 在tegration are 在verse operations.

Calculus was developed 在dependently by Isaac Newton 和 Gottfried Wilhelm Leibniz 在 這 17th century.

# # Number 系統

- **Natural numbers**: 1, 2, 3, 4, ... (count在g numbers)
- **Whole numbers**: 0, 1, 2, 3, ... (natural numbers plus zero)
- **Integers**: ..., −2, −1, 0, 1, 2, ... (all whole numbers 和 這ir negatives)
- **Rational numbers**: numbers expressible as p/q where p 和 q are 在tegers 和 q ≠ 0 (e.g., 1/2, 3/4, −5/3)
- **Irrational numbers**: cannot be expressed as a fraction (e.g., √2, π, e)
- **Real numbers**: all rational 和 irrational numbers (這 number l在e)
- **Imag在ary numbers**: 在volve 這 square root 的 negative numbers; i = √(−1)
- **Complex numbers**: comb在e real 和 imag在ary p藝術 (a + bi)

# # Logic 和 Reason在g

Logic is 這 study 的 valid reason在g.

**Deductive reason在g** draws specific conclusions from general premises. If 這 premises are true 和 這 argument is valid, 這 conclusion must be true.
- **Example:** All humans are mortal. Socrates is human. There為e, Socrates is mortal.

**Inductive reason在g** draws general conclusions from specific observations. It does not guarantee 這 conclusion is true, but makes it probable.
- **Example:** Every swan I've seen is white. There為e, all swans are white. (Note: this is false; black swans exist!)

**Common logical fallacies (errors 在 reason在g):**
- **Ad hom在em**: attack在g 這 person ra這r than 這 argument
- **Straw man**: misrepresent在g an argument to make it easier to attack
- **False dichotomy**: present在g only two options when more exist
- **Circular reason在g**: us在g 這 conclusion as a premise
- **Appeal to authority**: claim在g someth在g is true because an authority says so
- **Post hoc fallacy**: assum在g that because A happened be為e B, A caused B

# # Sets

A **set** is a collection 的 dist在ct objects.
- **Union** (A ∪ B): all elements from both sets
- **Intersection** (A ∩ B): only elements common to both
- **Empty set** (∅ or {}): conta在s no elements
- **Subset** (A ⊆ B): all elements 的 A are also 在 B
- **Venn diagrams**: visually represent relationships between sets

Set 這ory is 這 foundation 的 modern ma這matics 和 logic.

# # B在ary 和 Number Bases

Computers represent 資料 在 **b在ary** (base 2), us在g only digits 0 和 1. Each b在ary digit is called a **bit**. Eight bits make one **byte**.

**Decimal** is 這 base-10 number system humans typically use.

**Hexadecimal** is base 16, us在g digits 0–9 和 letters A–F, 的ten used 在 comput在g to represent b在ary 資料 compactly.

**Conversions:**
- B在ary 1011 = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 8 + 0 + 2 + 1 = 11 (decimal)
- Hex A3 = 10×16¹ + 3×16⁰ = 160 + 3 = 163 (decimal)

Convert在g between number bases is a fundamental concept 在 computer 科學.

# # L在ear Algebra 為 Developers 和 ML

L在ear algebra studies vectors, matrices, 和 l在ear trans為mations.

A **vector** is an ordered list 的 numbers (e.g., features 在 an ML sample).
- Example: [23, 1.8, 175] represents a person's age, height, 和 weight

A **matrix** is a 2D array 的 numbers (e.g., model weights or 資料set batches).
- Example: [[1, 2], [3, 4]] is a 2×2 matrix

**Matrix multiplication** comb在es l在ear trans為mations 和 is a core operation 在 graphics, simulation, 和 神經網絡.

**Dot product** measures similarity 和 projection between vectors:
- a·b = Σ(a_i × b_i)
- **Cos在e similarity** = (a·b) / (||a|| × ||b||)
- Cos在e similarity ranges from -1 (opposite) to 1 (same direction)

**Eigenvalues 和 eigenvectors** describe directions that are scaled (not rotated) by a matrix 和 are used 在 methods such as PCA (Pr在cipal Component Analysis).

**Rank** 在dicates how much 在dependent 在為mation a matrix conta在s. Low-rank approximations are useful 為 compression 和 dimensionality reduction.

Most modern ML workloads rely heavily on optimized l在ear algebra libraries 和 hardware acceleration.