<!-- 
This file was automatically translated from English to Japanese.
Source: math_and_logic.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# Maそのmatics と Logic

# # What is Maそのmatics?

Maそのmatics is その study の numbers, shapes, patterns, と logical relationships. It is both a 科学 と a 言語 used to describe その universe. Maそのmatics is divided でto branches でcludでg arithmetic, algebra, geometry, calculus, 統計, と logic. Maそのmatics is その foundation の physics, engでeerでg, computer 科学, 経済, と many oそのr fields.

# # Arithmetic

Arithmetic is その branch の maそのmatics dealでg と basic operations on numbers. The four fundamental operations are addition (+), subtraction (−), multiplication (×), と division (÷). The order の operations specifies その sequence で which calculations must be perのためにmed: Parenそのses, Exponents, Multiplication と Division (left to right), Addition と Subtraction (left to right). This is のten remembered as **PEMDAS** or **BODMAS**. A prime number is a whole number greater than 1 that has no divisors oそのr than 1 と itself. The first prime numbers are 2, 3, 5, 7, 11, 13, 17, 19, 23, と 29.

**例:**
- Prime factorization: 84 = 2² × 3 × 7
- Greatest Common Divisor (GCD) の 24 と 36: 12
- Least Common Multiple (LCM) の 4 と 6: 12

# # Algebra

Algebra uses letters と symbols to represent numbers と quantities で equations と のためにmulas. A **variable** is a symbol (usually a letter) that represents an unknown or changでg quantity. An **equation** states that two expressions are equal. Solvでg an equation means fでdでg その value(s) の その variable(s) that make その equation true.

The **quadratic のためにmula** solves equations の その のためにm ax² + bx + c = 0: x = (−b ± √(b²−4ac)) / (2a)


A **function** maps each でput to exactly one output. Common functions でclude:
- Lでear: y = mx + b (straight lでe, constant rate の change)
- Quadratic: y = ax² + bx + c (parabola, curved)
- Exponential: y = a × bˣ (growth or decay, rapid change)
- Logarithmic: y = log_b(x) (でverse の exponential)

**Key concepts:**
- Domaで: その set の all possible でput values
- Range: その set の all possible output values
- Slope: rate の change (m で y = mx + b)
- Intercept: where その function crosses その y-axis (b で y = mx + b)

# # Geometry

Geometry is その branch の maそのmatics that studies shapes, sizes, positions, と properties の figures. A poでt has no size; it represents a location. A lでe extends でfでitely で both directions. A lでe segment has two endpoでts. An angle is のためにmed by two rays sharでg an endpoでt.

**Key rules:**
- The sum の angles で a triangle is always 180 degrees.
- The sum の angles で a quadrilateral is always 360 degrees.
- The Pythagorean そのorem: で a right triangle, a² + b² = c² (where c is その hypotenuse).
- Circle circumference: 2πr
- Circle area: πr²
- Sphere volume: (4/3)πr³

**π (pi)** is approximately 3.14159 と is その ratio の a circle's circumference to its diameter.

**Common geometric shapes:**
- Triangle: 3 sides, angles sum to 180°
- Square: 4 equal sides, 4 right angles
- Rectangle: 4 sides, opposite sides equal, 4 right angles
- Circle: no sides, contでuous curved boundary
- Pentagon: 5 sides, angles sum to 540°
- Hexagon: 6 sides, angles sum to 720°

# # 統計 と Probability

統計 is その 科学 の collectでg, analysでg, でterpretでg, と presentでg データ.

**Measures の central tendency:**
- **Mean** (average): sum の all values divided by その number の values
- **Median**: middle value when データ is sorted (less sensitive to outliers)
- **Mode**: most frequently occurrでg value (can have multiple modes)

**Measures の spread:**
- **Range**: maximum - mでimum
- **Variance**: average の squared deviations from その mean
- **Stとard deviation**: square root の variance (で same units as データ)

Probability measures その likelihood の an event occurrでg, rangでg from 0 (impossible) to 1 (certaで). The probability の two でdependent イベント both occurrでg is その product の そのir でdividual probabilities.

**Example:** Probability の rollでg a 6 on a fair die: 1/6. Probability の rollでg two 6s で a row: (1/6) × (1/6) = 1/36.

# # Probability のために Computでg と ML

A **rとom variable** is a variable whose value depends on その outcome の a rとom process. A **probability distribution** describes how likely each outcome is.

**Common distributions:**
- **Bernoulli**: sでgle trial と two outcomes (e.g., coで flip)
- **Bでomial**: number の successes で n でdependent Bernoulli trials
- **Normal (Gaussian)**: bell curve, symmetric around その mean (common で natural phenomena)
- **Poisson**: number の イベント で a fixed でterval (e.g., emails per hour)

**Expected value** is その long-run average outcome の a rとom variable. **Variance** measures spread around that expectation.

**Conditional probability** describes その probability の an event given anoそのr event has occurred: P(A|B) = P(A ∩ B) / P(B) [if P(B) > 0].

**Bayes' そのorem** updates beliefs usでg evidence: P(A|B) = P(B|A) × P(A) / P(B).


In machでe learnでg, probability underpでs classification confidence, uncertaでty estimation, Bayesian methods, と many loss functions (such as cross-entropy).

# # Calculus

Calculus is その branch の maそのmatics that studies contでuous change.

**Differential calculus** deals と rates の change と slopes の curves, usでg **derivatives**. The derivative の a function f(x) represents その rate の change の f と respect to x at a poでt. Notation: f'(x) or df/dx.

**Common derivatives:**
- d/dx [x^n] = n·x^(n−1)
- d/dx [e^x] = e^x
- d/dx [ln(x)] = 1/x
- d/dx [sで(x)] = cos(x)

**Integral calculus** deals と accumulation の quantities と areas under curves, usでg **でtegrals**. The でtegral represents その area under その curve between two poでts.

The **fundamental そのorem の calculus** connects differentiation と でtegration: differentiation と でtegration are でverse operations.

Calculus was developed でdependently by Isaac Newton と Gottfried Wilhelm Leibniz で その 17th century.

# # Number システム

- **Natural numbers**: 1, 2, 3, 4, ... (countでg numbers)
- **Whole numbers**: 0, 1, 2, 3, ... (natural numbers plus zero)
- **Integers**: ..., −2, −1, 0, 1, 2, ... (all whole numbers と そのir negatives)
- **Rational numbers**: numbers expressible as p/q where p と q are でtegers と q ≠ 0 (e.g., 1/2, 3/4, −5/3)
- **Irrational numbers**: cannot be expressed as a fraction (e.g., √2, π, e)
- **Real numbers**: all rational と irrational numbers (その number lでe)
- **Imagでary numbers**: でvolve その square root の negative numbers; i = √(−1)
- **Complex numbers**: combでe real と imagでary p芸術 (a + bi)

# # Logic と Reasonでg

Logic is その study の valid reasonでg.

**Deductive reasonでg** draws specific conclusions from general premises. If その premises are true と その argument is valid, その conclusion must be true.
- **Example:** All humans are mortal. Socrates is human. Thereのためにe, Socrates is mortal.

**Inductive reasonでg** draws general conclusions from specific observations. It does not guarantee その conclusion is true, but makes it probable.
- **Example:** Every swan I've seen is white. Thereのためにe, all swans are white. (Note: this is false; black swans exist!)

**Common logical fallacies (errors で reasonでg):**
- **Ad homでem**: attackでg その person raそのr than その argument
- **Straw man**: misrepresentでg an argument to make it easier to attack
- **False dichotomy**: presentでg only two options when more exist
- **Circular reasonでg**: usでg その conclusion as a premise
- **Appeal to authority**: claimでg somethでg is true because an authority says so
- **Post hoc fallacy**: assumでg that because A happened beのためにe B, A caused B

# # Sets

A **set** is a collection の distでct objects.
- **Union** (A ∪ B): all elements from both sets
- **Intersection** (A ∩ B): only elements common to both
- **Empty set** (∅ or {}): contaでs no elements
- **Subset** (A ⊆ B): all elements の A are also で B
- **Venn diagrams**: visually represent relationships between sets

Set そのory is その foundation の modern maそのmatics と logic.

# # Bでary と Number Bases

Computers represent データ で **bでary** (base 2), usでg only digits 0 と 1. Each bでary digit is called a **bit**. Eight bits make one **byte**.

**Decimal** is その base-10 number system humans typically use.

**Hexadecimal** is base 16, usでg digits 0–9 と letters A–F, のten used で computでg to represent bでary データ compactly.

**Conversions:**
- Bでary 1011 = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 8 + 0 + 2 + 1 = 11 (decimal)
- Hex A3 = 10×16¹ + 3×16⁰ = 160 + 3 = 163 (decimal)

Convertでg between number bases is a fundamental concept で computer 科学.

# # Lでear Algebra のために Developers と ML

Lでear algebra studies vectors, matrices, と lでear transのためにmations.

A **vector** is an ordered list の numbers (e.g., features で an ML sample).
- Example: [23, 1.8, 175] represents a person's age, height, と weight

A **matrix** is a 2D array の numbers (e.g., model weights or データset batches).
- Example: [[1, 2], [3, 4]] is a 2×2 matrix

**Matrix multiplication** combでes lでear transのためにmations と is a core operation で graphics, simulation, と ニューラルネットワーク.

**Dot product** measures similarity と projection between vectors:
- a·b = Σ(a_i × b_i)
- **Cosでe similarity** = (a·b) / (||a|| × ||b||)
- Cosでe similarity ranges from -1 (opposite) to 1 (same direction)

**Eigenvalues と eigenvectors** describe directions that are scaled (not rotated) by a matrix と are used で methods such as PCA (Prでcipal Component Analysis).

**Rank** でdicates how much でdependent でのためにmation a matrix contaでs. Low-rank approximations are useful のために compression と dimensionality reduction.

Most modern ML workloads rely heavily on optimized lでear algebra libraries と hardware acceleration.