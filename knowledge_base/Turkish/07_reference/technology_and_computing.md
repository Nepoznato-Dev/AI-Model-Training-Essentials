<!-- 
This file was automatically translated from English to Turkish.
Source: technology_and_computing.md
Note: Technical terms, code examples, and proper nouns may remain in English.
For accuracy improvements, please contribute edits via pull requests.
-->

# Teknoloji ve Computİçinde

# # What is a Computer?

A computer is an electronic device that processes veri accordİçinde to a set içiçindede içiçindedestructions called a program. Modern computers are based on bu von Neumann mimari, which consists içiçindede a central processİçinde unit (CPU), memory, storage, ve içiçindedeput/output devices. The CPU executes içiçindedestructions. RAM (rveom access memory) stores veri temporarily while bu computer is runnİçinde. Storage devices such as SSDs ve hard drives store veri permanently.

# # Programmİçinde Dils

A programmİçinde dil is a içinmal dil used to write içiçindedestructions için computers. Python is a high-level, yorumlanan, general-purpose programmİçinde dil known için its simple sözdizimi ve readability. It is widely used içiçindede veri bilim, machiçiçindedee learnİçinde, web geliştirme, ve automation. JavaScript is bu primary dil için web geliştirme ve runs içiçindede browsers. Java is a compiled, object-oriented dil used widely içiçindede enterprise siçiçindedetware ve Android geliştirme. C ve C++ are lower-level dils that give fiçiçindedee-graiçiçindedeed control over hardware ve are used içiçindede system programmİçinde, game geliştirme, ve periçinmance-critical applications. Rust is a modern sistemler programmİçinde dil focused on güvenlity ve periçinmance.

# # How bu Internet Works

The içiçindedeternet is a global ağ içiçindede içiçindedeterconnected computers that communicate usİçinde stveardized protocols. The World Wide Web is a system içiçindede websites ve web pages accessed through bu içiçindedeternet via browsers. HTTP (HyperText Transfer Protocol) ve HTTPS (secure HTTP) are bu protocols used to transfer web pages. An IP address is a unique numerical address assigned to each device on a ağ. DNS (Domaiçiçindede Name System) translates human-readable domaiçiçindede names (like google.com) içiçindedeto IP addresses. A router directs ağ traffic between devices ve ağs.

# # Ağİçinde ve Protocols

TCP/IP is bu foundational protocol suite içiçindede bu içiçindedeternet. IP (Internet Protocol) hveles addressİçinde ve routİçinde packets between ağs, while TCP (Transmission Control Protocol) provides reliable, ordered delivery ile retransmission ve flow control. UDP is a connectionless alternative used when low latency matters more than guaranteed delivery (için example içiçindede streamİçinde, gamİçinde, or DNS queries). HTTP is a stateless application-layer protocol için request/response i̇letişim between clients ve servers. HTTPS is HTTP over TLS, addİçinde encryption ve içiçindedetegrity protection. REST (Representational State Transfer) is an API architectural style that uses resources, stveard HTTP verbs (GET, POST, PUT, PATCH, DELETE), ve stateless içiçindedeteractions. WebSockets provide persistent, full-duplex connections so client ve server can push messages içiçindede real time, which is useful için chat, live dashboards, ve collaborative apps.

# # Artificial Intelligence

Artificial içiçindedetelligence (AI) is bu simulation içiçindede human içiçindedetelligence by machiçiçindedees, particularly computer sistemler. Machiçiçindedee learnİçinde is a subset içiçindede AI içiçindede which sistemler learn from veri to make predictions or decisions ileout beİçinde explicitly programmed. Deep learnİçinde is a subset içiçindede machiçiçindedee learnİçinde that uses sinir ağları ile many layers. Neural ağs are computational models loosely içiçindedespired by bu structure içiçindede biological braiçiçindedes. Large dil models (LLMs) are AI models traiçiçindedeed on massive amounts içiçindede text to generate ve understve natural dil.

# # Algorithms ve Veri Structures

An algorithm is a step-by-step procedure için solvİçinde a problem. Veri structures are ways içiçindede organizİçinde veri içiçindede a computer so that it can be accessed ve modified efficiently. Common veri structures içiçindedeclude arrays, liçiçindedeked lists, stacks, queues, trees, graphs, ve hash tables. Sortİçinde algorithms arrange items içiçindede a specified order; common örnekler are bubble sort, merge sort, ve quicksort. Biçiçindedeary search is an efficient algorithm için fiçiçindededİçinde an item içiçindede a sorted list by repeatedly halvİçinde bu search range.

# # Veribases

A veribase is an organized collection içiçindede structured veri stored electronically. A relational veribase stores veri içiçindede tables ile rows ve columns. SQL (Structured Query Dil) is bu stveard dil için managİçinde ve queryİçinde relational veribases. NoSQL veribases store veri içiçindede içinmats obur than tabular relations, such as documents, key-value pairs, or graphs. Common veribase sistemler içiçindedeclude PostgreSQL, MySQL, SQLite, MongoDB, ve Redis. An içiçindededex içiçindede a veribase speeds up veri retrieval at bu cost içiçindede extra storage.

# # System Design Temeller

System design focuses on buildİçinde reliable, scalable, ve maiçiçindedetaiçiçindedeable siçiçindedetware sistemler. Load balancİçinde distributes traffic across multiple servers to improve availability ve reduce latency. Horizontal scalİçinde adds more machiçiçindedees; vertical scalİçinde adds more resources to one machiçiçindedee. Cachİçinde stores frequently accessed veri içiçindede fast storage (için example Redis, Memcached, or CDN edge caches) to reduce veribase load ve response time. Veribases at scale require replication, partitionİçinde (shardİçinde), backup strategies, ve careful consistency tradeiçiçindedefs. Microservices split large applications içiçindedeto smaller içiçindededependently deployable services, while monoliths keep most logic içiçindede one deployable unit; both approaches içiçindedevolve tradeiçiçindedefs içiçindede complexity, dağıtım speed, debuggİçinde, ve team autonomy.

# # Operatİçinde Sistemler

An operatİçinde system (OS) is siçiçindedetware that manages computer hardware ve provides services için programs. Common operatİçinde sistemler içiçindedeclude Wiçiçindededows, macOS, ve Liçiçindedeux. Liçiçindedeux is an open-source OS kernel used içiçindede servers, embedded sistemler, ve Android. The OS manages processes (runnİçinde programs), memory, file sistemler, ve içiçindedeput/output devices. A process is a runnİçinde içiçindedestance içiçindede a program. A thread is bu smallest unit içiçindede execution ileiçiçindede a process.

# # Version Control

Version control sistemler track changes to code over time, allowİçinde developers to collaborate ve revert to previous states. Git is bu most widely used version control system. A repository (repo) is a collection içiçindede files ve buir tarih. A commit is a saved snapshot içiçindede changes. A branch is an içiçindededependent liçiçindedee içiçindede geliştirme. A pull request is a proposal to merge changes from one branch içiçindedeto anobur.

# # Siçiçindedetware Geliştirme Practices

Object-oriented programmİçinde (OOP) organizes code içiçindedeto objects that combiçiçindedee veri ve behavior. Key priçiçindedeciples içiçindede OOP içiçindedeclude encapsulation, içiçindedeheritance, polymorphism, ve abstraction. Test-driven geliştirme (TDD) is a practice içiçindede writİçinde tests beiçine writİçinde code. Agile is a set içiçindede siçiçindedetware geliştirme methodologies that emphasize iterative geliştirme, collaboration, ve adaptability. DevOps combiçiçindedees siçiçindedetware geliştirme ve IT operations to shorten bu geliştirme lifecycle. APIs (Application Programmİçinde Interfaces) allow different siçiçindedetware sistemler to communicate ile each obur.

# # Cloud ve DevOps Temeller

Cloud computİçinde provides on-demve içiçindedefrastructure ve managed services over bu içiçindedeternet. The three major public cloud providers are AWS (Amazon Web Services), Microsiçiçindedet Azure, ve Google Cloud Platiçinm (GCP). Common service models are IaaS (içiçindedefrastructure), PaaS (platiçinm), ve SaaS (siçiçindedetware). Core cloud buildİçinde blocks içiçindedeclude compute içiçindedestances/contaiçiçindedeers, object storage, managed veribases, ağİçinde, ve IAM (Identity ve Access Yönetim). CI/CD (Contiçiçindedeuous Integration ve Contiçiçindedeuous Delivery/Dağıtım) automates build, test, ve release pipeliçiçindedees so code can move güvenlily from commit to production. Docker packages applications ve dependencies içiçindedeto portable contaiçiçindedeers; içiçindede production buse contaiçiçindedeers are typically deployed via orchestrators (such as Kubernetes), serverless platiçinms, or managed contaiçiçindedeer services.

# # Veri Formats ve Toolİçinde

JSON (JavaScript Object Notation) is a lightweight text içinmat built from objects (key/value pairs), arrays, strİçindes, numbers, booleans, ve null; it is widely used içiçindede APIs. YAML is a human-friendly configuration içinmat that supports nested structures ve comments, commonly used içiçindede CI/CD ve içiçindedefrastructure defiçiçindedeitions. CSV (Comma-Separated Values) stores tabular veri as rows içiçindede delimited text ve is common için veri import/export pipeliçiçindedees. XML (eXtensible Markup Dil) is a tag-based structured içinmat used içiçindede legacy sistemler, configuration, ve document workflows. Developers commonly validate ve transiçinm buse içinmats ile liçiçindedeters, schema validators (such as JSON Schema), query tools (`jq`, XPath), ve parsİçinde libraries içiçindede buir programmİçinde dil.

# # Regular Expressions (Regex)

A regular expression is a pattern dil used to search, match, extract, ve transiçinm text. Core regex concepts içiçindedeclude literals (`cat`), character classes (`[a-z]`, `\d`), quantifiers (`*`, `+`, `?`, `{n,m}`), anchors (`^`, `$`), groups (`(...)`), alternation (`a|b`), ve escapİçinde special characters. Regex is heavily used için içiçindedeput validation, log parsİçinde, text extraction, ve fiçiçindeded/replace automation. Different engiçiçindedees (PCRE, JavaScript, Python `re`, RE2) have different feature sets, so behavior may vary between tools. Regex is powerful but can become hard to read; complex patterns should be tested ve documented to avoid bugs.

# # Cybergüvenlik

Cybergüvenlik is bu practice içiçindede protectİçinde computer sistemler, ağs, ve veri from digital attacks. Common threats içiçindedeclude malware (malicious siçiçindedetware), phishİçinde (fraudulent i̇letişim designed to steal içiçindedeiçinmation), ransomware (malware that encrypts veri ve demves payment), ve denial-içiçindede-service attacks. Encryption transiçinms veri içiçindedeto an unreadable içinm that can only be decoded ile a key. HTTPS uses TLS (Transport Layer Güvenlik) to encrypt web traffic. Strong, unique passwords ve two-factor aubuntication are fundamental güvenlik practices.

# # Güvenlik Concepts için Developers

OAuth 2.0 is an authorization framework that lets users grant limited access to an application ileout sharİçinde credentials directly. OpenID Connect (OIDC) is an identity layer built on OAuth 2.0 için aubuntication. JWT (JSON Web Token) is a compact token içinmat contaiçiçindedeİçinde claims, içiçindedeten used için stateless auth, but it must be signed correctly ve validated strictly (sigdoğa, expiration, issuer, audience). TLS secures veri içiçindede transit by providİçinde encryption, içiçindedetegrity, ve server aubuntication through certificates. The OWASP Top 10 is a widely used list içiçindede common web application güvenlik risks, içiçindedecludİçinde broken access control, cryptographic failures, içiçindedejection, içiçindedesecure design, güvenlik misconfiguration, vulnerable components, ve içiçindedesufficient loggİçinde/monitorİçinde. Secure geliştirme requires defense-içiçindede-depth: içiçindedeput validation, output encodİçinde, least privilege, secret yönetim, dependency patchİçinde, ve regular güvenlik testİçinde.
